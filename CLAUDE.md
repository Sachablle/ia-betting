# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

**ValueBet** — a sports betting analytics dashboard. Shows upcoming football and basketball fixtures with team stats, and highlights value bets by comparing bookmaker odds against Pinnacle's sharp lines.

## Running the app

Two processes must run concurrently:

```bash
# Frontend (React + Vite, port 5173)
npm run dev

# Backend (Express, port 3001) — in a separate terminal
cd backend && npm run dev
```

Build for production:
```bash
npm run build   # outputs to dist/
```

---

## Architecture

### Frontend (`src/`)
React 18 SPA built with Vite. Vite proxies all `/api` requests to `http://localhost:3001`.

**Routing** (`App.jsx`): Sidebar + `<Routes>` for `/football`, `/football/:id`, `/basketball`, `/basketball/:id`.

**Data sources — two parallel tracks:**
1. **Static fixture data** (`src/utils/fixtures.js`, `src/utils/basketball.js`): hardcoded match data used by the current UI pages. This is what the pages actually render.
2. **Live API utilities** (`src/utils/api.js`): fetch functions for the backend APIs. Exist but not yet wired into the main pages.

**Key components:**
- `OddsTable.jsx` — renders h2h and BTTS odds per bookmaker, highlights best odds and value edges vs Pinnacle
- `StatBar.jsx` — side-by-side comparative stat bar for match detail pages
- `FormStrip.jsx` — W/D/L recent form badges
- `TeamLogo.jsx` — loads logos from API-Sports CDN: `https://media.api-sports.io/football/teams/{logoId}.png`

All dates/numbers are formatted in French locale (`fr-FR`) via `src/utils/formatters.js`.

### Backend (`backend/server.js`)
Single Express file. All external API calls happen here; the frontend never calls third-party APIs directly.

**Routes:**
- `GET /api/health`
- `GET /api/fd/results` — football-data.org: final scores for the 5 leagues, ±2/1 day window (`_getFdLeaguesResults()`, 30-min cache). Sole purpose: settlement (`resolveCompletedFootballAlerts()` frontend + `runAutoSettle()`/near-miss backend) — `/api/fd/matches` below never returns finished matches (`?status=SCHEDULED`), so this is a separate route on purpose.
- `GET /api/fd/matches` — football-data.org: next 5 fixtures + standings for 5 leagues. 30-min cache. 200ms delay between league requests (10 req/min limit). Sole source for the 5-league fixture list (Carte du Monde hub, `WorldMapPage.jsx`, and `MatchDetailPage.jsx` via `useFootballFixtures.js`) since 12 juillet 2026 — API-Football (`v3.football.api-sports.io`) was removed entirely from the project at that point (dead weight: its `/api/football/matches` route had been gated off behind an unset `FOOTBALL_MATCHES_ENABLED` flag since the feature's original commit, `/api/football/h2h` was never called by any page, `src/utils/api.js` that wrapped both was never imported anywhere). **Reintroduced 22 juillet 2026** (Pro plan, see "api-football Pro" below) as a supplement for injuries/xG/snapshot on those same 5 leagues + Brasileirão, and **as the sole fixture/odds/model source** for the 3 EU cup competitions added 23 juillet 2026 (see "Coupes d'Europe" below) — football-data.org still owns the 5-league + CDM + Brasileirão fixture lists themselves, api-football never replaces it there.
- `GET /api/odds` — The Odds API: h2h + btts markets, bookmakers: `pinnacle, betfair_ex_eu, unibet_eu, betclic`.
- `GET /api/alerts` — value bet alerts: edges above `VALUE_THRESHOLD` (default 2%).
- `GET /api/nba/scoreboard` — ESPN live NBA scoreboard. Polled every 30s if live game active, 5min otherwise.
- `GET /api/nba/players/:teamId` — ESPN roster for a team. Cache 6h.
- `GET /api/nba/teamschedule/:teamId` — ESPN team schedule (recent + upcoming games). Cache 6h.
- `GET /api/nba/playergamelog/:playerId` — ESPN player game log (last ~15 games). Cache 6h.
- `GET /api/nba/boxscore?date=ISO&home=ABR&away=ABR` — ESPN box score for a completed game. Cache 5min (to pick up NBA post-game stat corrections). Date converted UTC→ET. Response keys use standard abbreviations (SAS, NYK…).
- `POST /api/basketball/result` — team win probability via `computeTeamWinProb` (see below). Body: `{homeGames, awayGames, gameDate, round, league, homePlayers, awayPlayers}`. Used both by `generateBackgroundAlerts()` (alerts) and `BasketballDetailPage.jsx`'s "Modèle 1X2" widget (display) — same function, single source of truth.
- `GET /api/football/cdm/poolavg?fixtureId=fdcdm_xxx` — CDM pool average `avgGF`/`avgGA`. With `fixtureId`, returns the value **frozen** for that match (see CDM section below) if already generated; without it, the live current pool average.
- `GET /api/football/teamxgstats?league=X&team=Y&date=Z` — recent xG/xGA/shots/possession for a team via api-football Pro (see "api-football Pro" below). Returns `{found, xG, xGA, shotsPerGame, shotsOnTarget, possession, games}`.
- `GET /api/football/projections-snapshot/:fixtureId` — real backend-computed BTTS/O-U/1X2 for a fixture, frozen at first generation (see "Football snapshot" below). Returns `{found, bttsProb, pOver25, pUnder25, pHome, pDraw, pAway, lambdaHome, lambdaAway, estimated, savedAt}`.
- `GET /api/outrights` — competition-winner odds (Ligue 1, PL, La Liga, Serie A, Bundesliga, NBA, WNBA). `?refresh=1` forces a re-scrape (see Outrights section below).
- `GET /api/mlb/preview` — MLB fixtures + Betclic/Unibet Total runs odds + model probabilities per line, merged in one call (see "MLB" section below). `GET /api/mlb/lineups` — RotoWire confirmed/probable lineups. `GET /api/mlb/teams` / `GET /api/mlb/roster/:teamId` — MLB Stats API roster (Base de données page). `GET /api/mlb/games` / `GET /api/mlb/teamstats` / `GET /api/mlb/odds` / `GET /api/mlb/matches` — lower-level building blocks used internally by `/preview`.
- `GET /api/analysis/near-miss` — basketball player-props near-miss log (pts/reb/ast/3pts NBA+WNBA). `GET /api/analysis/near-miss-football` — football BTTS/O-U/Résultat near-miss log. `GET /api/analysis/near-miss-basket-markets` — basketball Résultat/Total O-U/Écart H2H near-miss log (NBA/WNBA/ACB/BBL/LegaA, added 24 juillet 2026, see "Near-miss tracking" below). `GET /api/analysis/near-miss-mlb` — MLB Total runs near-miss log, unfiltered (see "MLB" section below).

**Value bet calculation:**
```
fair_prob = (1/pinnacle_odds) / overround   // overround = sum of 1/odds for all outcomes
edge = bookmaker_odds * fair_prob - 1
```

**Player props alerts — ladder de lignes alternatives (18 juin 2026, `findLadderAlternative()` in `server.js`):**
For NBA/WNBA/EU basket player props (pts/reb/ast/tpm), each player normally gets evaluated on a single "reference" bookmaker line (`bks.unibet[stat] ?? bks.winamax[stat]`). When that line's probability+edge already clear the floor but its odds don't reach the `1.60` minimum on Unibet/Betclic, `findLadderAlternative()` searches the full line ladder already scraped for `PlayerLinesPage.jsx` (`bks.unibetAllLines[stat]` / `bks.betclicAllLines[stat]`) for a neighboring line in the direction that *raises* the odds on the favored side (higher line for Over, lower for Under). The probability is recalculated from scratch at each candidate line (never reused from the reference line) via the same `probAtLeast`/`displayProb` calibration; all the existing gates (floor, edge, WNBA reb-margin/ast-cap/tpm-min-avg, NBA L2-above-line) are re-checked against the candidate line too. Among candidates that pass both the odds and probability gates, the one with the **highest probability** wins. Resulting alerts carry `lineSource: 'ladder'` and use the alternate `line`/odds instead of the reference one — same one-alert-per-player-per-stat rule, just possibly on a different line than the one shown by default elsewhere in the UI. Applied at all 5 emission sites: NBA live + frozen, WNBA live + frozen, EU (acb/bbl/legaa).

**Player props — match-page display unified with the real alert engine (1er août 2026):** `BasketballDetailPage.jsx`'s "Analyse Props" section used to run its own hand-copied reimplementation of the probability model (local `computeEstimate`/`probAtLeast`/`calcStd` + ~20 factor helpers), which had drifted from the real backend model in `compute.js` (missing winsorization, missing the `roleShrunk` branch, never using real per-position opponent defense) — confirmed on a real case (Natasha Cloud showing 65% assists on the match page while the alert engine never even logged a near-qualifying attempt for her). Fixed the same way Résultat/Total/Spread already were: three new pure orchestration functions in `server.js` (`getPlayerProjectionsNBA`/`getPlayerProjectionsWNBA`/`getPlayerProjectionsEU`, placed after `runEUPropsAlerts()` and before `generateBackgroundAlerts()`) reuse the exact same real functions the alert engine calls (`computeEstimate`/`computeEUEstimate`, `displayProb`, `oppInjuryEffect`, `computeRedist`) — only the *live* computation path is replicated (not the frozen/snapshot path, since an on-demand page load always wants a fresh number), and deliberately **none** of the alert-decision logic (floors, season-margin gates, min-edge, ladder-alternate search, near-miss logging) — a display doesn't need to know if a number would clear an alert threshold. Exposed via `POST /api/basketball/player-projections` (same request/response shape family as `/result`/`/total`/`/spread`), called by `PropsSection` for `BACKEND_MODEL_LEAGUES = ['nba','wnba','acb','bbl','legaa']` instead of its local calc. `compute.js`'s `computeEstimate` also now returns a `factors` array (the same internal pace/defense/rest/etc. values it already computed, just exposed) to keep the "Facteurs du modèle" breakdown panel working with real numbers. **Euroleague and LNB keep the old local calculation unchanged** — neither has ever had a real backend alert engine for props (LNB excluded from `runEUPropsAlerts` for unreliable gamelogs, Euroleague never covered at all), so there is nothing to unify for them and zero regression risk there. Verified: 3/4 real players cross-checked against the live `bg-log` matched exactly (4th within 2%, live data refresh between calls, not a formula gap); zero lines changed inside `generateBackgroundAlerts()` or its 5 existing prop call sites (diff-reviewed); live alerts identical before/after.

**Basketball Résultat (victoire équipe) alerts — `computeTeamWinProb()` in `server.js` (19 juin 2026, replaces EarlyWin):**
EarlyWin (the old "team leads by +X or wins" alert) used an uncalibrated empirical formula (`P(EW) = 0.70×pWin + 0.15×(1-pWin)`) and was never actually wired to the frontend sync pipeline (`syncAlerts.js` had no handler for `type:'earlywin'` — the only alerts ever shown came from a separate, redundant client-side generator in `BasketballDetailPage.jsx`). Replaced end-to-end with an independent team-strength model:
- `computeTeamWinProb({homeGames, awayGames, gameDate, round, isWNBA, homeOutPenalty, awayOutPenalty})` — net rating (`ptsScored - ptsAllowed`, EWA-weighted) per team, adjusted by `getRestFactor`/`getScheduleDensityFactor` (from `compute.js`), `+HOME_COURT_PTS` (2.5, scaled to the league via the same point-scale rescale as EU), damped by `getPlayoffFactorTotalBg` in playoffs, converted to win probability via `tCDF4` (margin/std). `calcKeyPlayerOutPenalty()` removes `KEY_PLAYER_OUT_FACTOR=0.4` × season pts for any roster player with `injury==='Out'` and `stats.pts >= KEY_PLAYER_PTS (15)`.
- Generated in `generateBackgroundAlerts()` for NBA/WNBA (section 4b-bis) and EU acb/bbl/legaa (section 4b, schedules rescaled to NBA-point-equivalent like the EU props engine) — gated on the **h2h market** (not the old, rarer "earlywin/avance" market), `RESULT_ALERT_PROB = 0.80`, `RESULT_MIN_ODDS = 1.50`, bookmaker loop (`Object.entries(h2hBks)`) filters out winamax since 22 juin 2026 (same as basket props/total/football).
- Output: `type: 'basketball_result'` (`id: ${eventId}_{league}_result_{side}` for NBA/WNBA, `${eventId}_eu_result_{side}` for EU), `direction: 'home'|'away'`, `probability`, `margin` (expected point margin), `edge` vs vig-removed h2h odds.
- Frontend: `syncBasketballResultAlerts()` (`src/utils/syncAlerts.js`) merges into `basketball_result_alerts` localStorage key. `BasketballResultCard` (`PlaceBetPage.jsx`) renders pending cards.
- **Single source of truth**: `BasketballDetailPage.jsx`'s "Modèle 1X2" display widget calls `POST /api/basketball/result` with its already-fetched schedules/rosters — same function as the alert generator, so the page never shows a probability that disagrees with the alert (this exact kind of divergence, for a different bug, was the whole subject of the 19 juin session — see [[project_refonte_validation_watch]] memory).

**Basketball Total O/U — tendance H2H resserrée aux vraies séries (22 juillet 2026, `computeGameTotalFull()` in `server.js`):**
`h2hFactor` (added 22 juin for the ACB Barcelone-Valence playoff-series case: totals dropping game after game as defenses adjust) used to extrapolate a trend from any 2 head-to-head meetings in the season, regardless of how far apart in time they were. Real case: Seattle-Minnesota WNBA had only 2 meetings 6 weeks apart (6 juin, 156 pts → 21 juillet, 207 pts) — extrapolated into a wrongful +15% (capped) push on the 22 juillet rematch, which then collapsed to ~160 pts (alert lost). Fix: `H2H_SEQUENCE_MAX_DAYS = 14` — the trend now only extrapolates if the last H2H meeting is ≤14 days before the current game AND the 2 meetings used are ≤14 days apart (genuine series/back-to-back); otherwise `h2hFactor` stays neutral (1.0), same as the pre-existing default with <2 meetings. Single function shared by `game_total` alerts (NBA/WNBA/EU) and the `/api/basketball/total` display widget — one fix point, no divergence possible.

**Football BTTS / Over-Under alerts (`backend/computeFootball.js` + `generateBackgroundAlerts()` section 4d):**
Poisson model, generated every 20min alongside NBA/EU basket alerts — no need to visit `MatchDetailPage` for an alert to be created.
- `computeLambdas({homeGF, homeGA, homePlayed, awayGF, awayGA, awayPlayed, leagueAvgGoals, avgGF, avgGA, homeAdv=1.10})` — attack/defense factors normalized by `avgGF`/`avgGA` (falling back to `leagueAvgGoals` if omitted), each factor passed through `shrinkFactor()` (see below), λ rescaled by `leagueAvgGoals` × home advantage. Returns `null` if sample too small.
- `shrinkFactor(rawFactor, games, k=SHRINK_K=5)` (25 juin 2026) — pulls a small-sample attack/defense factor toward 1.0 ("average team") proportionally to `confidence = games/(games+k)`: at 3 games, only ~37% of the raw deviation from 1.0 is trusted; at 20 games, ~80%. Literature-style constant, not locally calibrated (same caveat as `DIXON_COLES_RHO` below) — mainly matters for CDM squads early in the tournament with only 3-4 real/qualifier matches on file. Mirrored client-side in `MatchDetailPage.jsx` (`computeCdmBTTS`, same `SHRINK_K`).
- **Dixon-Coles correlation** (25 juin 2026) — independent Poisson underestimates low, correlated scorelines (0-0, 1-0, 0-1, 1-1: a team protecting a lead closes the game down) relative to real results. `computeScoreGrid(λh, λa, rho=DIXON_COLES_RHO=0.10, kMax=10)` builds the joint P(i,j) grid, multiplies the 4 low-score cells by `dixonColesTau(x,y,λh,λa,rho)` (standard Dixon & Coles 1997 τ — reduces (0,0)/(1,1), boosts (0,1)/(1,0)), then renormalizes the whole grid to sum to 1. `ρ=0` reproduces the old independent-Poisson numbers exactly (validated to ~1e-7) — it's the safety net if this ever needs to be turned off. `ρ=0.10` is a textbook reference value, **not calibrated on our own results** (sample too small for a reliable MLE fit).
- `computeBTTSProb(λh, λa, rho=DIXON_COLES_RHO)` — sum of `computeScoreGrid` cells where `i≥1` and `j≥1` (replaces the old `(1-P(home=0))*(1-P(away=0))` closed form, which assumed independence).
- `computeOUProb(λh, λa, line, rho=DIXON_COLES_RHO)` — sum of grid cells where `i+j ≤ floor(line)` → `pUnder`, `pOver = 1-pUnder`. At the standard 2.5 line, the 4 Dixon-Coles cells all sit inside the same under-bucket, so the net effect on O/U is small; the correction mainly reshuffles BTTS and 1X2 draw probability.
- `compute1X2Probs(λh, λa, kMax=10, rho=DIXON_COLES_RHO)` — same grid, sums `P(i,j)` by `i>j` (home win), `i===j` (draw), `i<j` (away win) → `{pHome, pDraw, pAway}`. All three (BTTS/O-U/1X2) now derive from one shared grid for internal consistency.
- **Neutral home advantage — CDM only** (25 juin 2026): `homeAdv` is no longer a blanket `1.10` for World Cup fixtures. `CDM_HOST_NATIONS = new Set(['United States','Canada','Mexico'])` (2026 hosts) — `homeAdv: CDM_HOST_NATIONS.has(g.home.name) ? 1.10 : 1.0` is computed when building each CDM fixture and passed into `computeLambdas`; everything else (the "home" label is arbitrary fixture-list order at a neutral tournament venue) gets `1.0`. The 5 domestic leagues are unaffected (no `homeAdv` set on their fixture objects → `computeLambdas` default `1.10` applies, same as before). Mirrored in `MatchDetailPage.jsx` (`computeCdmBTTS`, `CDM_HOST_NATIONS`, 4th arg = `fixture.home.name`).
- Fixtures source: `_fdCache.matches` (5 leagues, `fixtureId = fd_${id}`, `leagueAvgGoals = FB_LEAGUE_AVG_GOALS[league]` — `{ligue1:1.35, pl:1.45, laliga:1.35, bundes:1.55, seriea:1.35}`, `avgGF`/`avgGA` omitted — closed league, GF≈GA on average, single constant is fine) + `_cdmCache.games` `STATUS_SCHEDULED` (`fixtureId = fdcdm_${id}`, `leagueAvgGoals = CDM_AVG_GOALS = 1.30`, `avgGF`/`avgGA` computed **dynamically** as the average `goalsFor`/`goalsAgainst` across all teams in the current CDM fixture pool — `CDM_AVG_GF=2.15`/`CDM_AVG_GA=0.78` are fallback-only if no stats available, requires `games >= 3` both teams via `/api/football/cdm/teamstats/:name`).
  - **Important**: `/api/football/cdm/teamstats/:name` returns `goalsFor`/`goalsAgainst` as already-weighted **per-match averages** (not totals). `computeLambdas` expects totals (it divides by `played`), so the CDM fixture builder multiplies back by `games`: `homeGF: hs.goalsFor * hs.games, homePlayed: hs.games`. Don't pass the raw averages directly — λ collapses to ~0 and every match falsely alerts "Under" at 100%.
  - **CDM attack/defense asymmetry**: qualified WC teams' recent friendlies/qualifiers are mostly against weaker opponents, so average GF is far above average GA across the squad pool. Normalizing both attack and defense by the same `CDM_AVG_GOALS=1.30` crushed every λ below 1.7 → 100% "Under" regardless of matchup. Fix: `avgGF`/`avgGA` normalize attack/defense separately, `CDM_AVG_GOALS` only rescales the final λ (home/away split + home advantage). The 5 leagues are unaffected (no `avgGF`/`avgGA` passed → same behavior as before).
  - **Dynamic avgGF/avgGA**: computed each `generateBackgroundAlerts()` run as the mean `goalsFor`/`goalsAgainst` over every team (`games >= 3`) appearing in the scheduled CDM fixtures — self-recalibrates as the team pool changes (knockout stage, new qualifiers) instead of relying on a stale hardcoded sample. A fixed `avgGA=0.65` (sample of ~13 teams) was initially used but proved too low once weaker teams (Haiti, Curaçao, NZ, Scotland — GA ~1.0-1.4) were included, inflating defense factors and producing unrealistic BTTS alerts (e.g. Germany vs Curaçao 75%). The dynamic average (~0.83 on the current pool) removed those overconfident BTTS calls entirely.
  - **Fix 18 juin 2026 — matchs CDM réels manquants des stats équipe**: `/api/football/cdm/teamstats/:name` ne récupérait que `fifa.friendly` + `fifa.worldq.{conf}` (amicaux + qualifs), jamais `fifa.world` (le calendrier de la Coupe du Monde elle-même). Une fois le tournoi commencé, les matchs de poule déjà joués (J1, J2…) étaient donc invisibles du modèle, qui continuait de tourner sur des amicaux/qualifs d'avant-tournoi (ex: Canada vs Qatar J2 calculé sans voir Canada 1-1 Bosnie ni Qatar 1-1 Suisse, leurs vrais matchs de poule précédents). Fix : ajout de `fetchEspnSoccerSchedule('fifa.world', espnId)`, résultats tagués `type: 'worldcup'`, poids `1.5` (au-dessus des qualifs `1.3` et amicaux `0.85`) — un match de poule réel est la donnée la plus représentative une fois le tournoi lancé (effectif et forme actuels, niveau d'adversité CDM).
  - **Note — alias nom manquant**: `ESPN_CDM` utilise `turkiye` (nom FIFA) mais la source `_cdmCache` (football-data.org) renvoie `Turkey` → 404 sur `/api/football/cdm/teamstats/Turkey`, cette équipe est donc exclue de la moyenne du pool et n'a pas d'alertes générées. Pas encore corrigé (ajouter `turkey: 'turkiye'` à `CDM_NAME_ALIASES` si besoin).
  - **Fix 19 juin 2026 — avgGF/avgGA figés par match (`freezeCdmPoolAvg()`)** : avant ce fix, `avgGF`/`avgGA` étaient recalculés à *chaque* cycle de 20min sur le pool de matchs encore programmés dans les 24h — un pool qui change constamment fait dériver fortement la probabilité d'un match déjà suivi, sans qu'aucune donnée de ce match lui-même ait changé (cas réel : BTTS Mexique-Corée du Sud généré à 76%, recalculé à 18% quelques heures plus tard, alors qu'aucune des deux équipes n'avait rejoué). Fix : `avgGF`/`avgGA` sont maintenant figés par `fixtureId` dès la 1ère génération (`_cdmFixturePoolAvg`, persisté dans `backend/cache/cdm_fixture_poolavg.json`), réutilisés tels quels aux cycles suivants tant que le match n'a pas eu lieu. Exposé côté frontend via `GET /api/football/cdm/poolavg?fixtureId=...` (`MatchDetailPage.jsx` l'utilise déjà).
- Alert thresholds (raised 27 juin 2026, see "Session 27 juin" below): `FB_BTTS_ALERT_PROB = 0.70`, `FB_OU_ALERT_PROB = 0.70`, `FB_RESULT_ALERT_PROB = 0.70`, `FB_BTTS_OU_MIN_ODDS = 1.60`, `FB_RESULT_MIN_ODDS = 1.50` (best of unibet/betclic on the proposed direction — winamax excluded from the threshold check since 22 juin 2026, same as basket props/total/result; winamax odds still shown on the card for reference). O/U tries line `2.5` then falls back to `1.5`, one alert per fixture.
- Output: `type: 'football_btts'` (`id: ${fixtureId}_btts_yes`, same id scheme as the client-generated `fb_btts_alerts` → natural dedup), `type: 'football_total'` (`id: ${fixtureId}_total_${line}`, includes `estimated` = `λhome+λaway` expected total goals), and `type: 'football_result'` (`id: ${fixtureId}_result_${direction}`, `direction ∈ {home,draw,away}`, includes `probability` and optional `edge` vs vig-removed h2h odds).
- **Football Résultat 1X2 (15 juin 2026)**: each outcome (home/draw/away) is treated as an *independent binary yes/no proposition* (same framing as BTTS), not a 3-way mutually-exclusive pick — since `2 × FB_RESULT_ALERT_PROB > 1`, at most one outcome can qualify per fixture, so still "one alert per fixture". h2h odds come from the same scraping merge as BTTS/O-U (`_mergeFootballBookmaker` → `oddsMatch.markets.h2h.bookmakers[bk] = {home, draw, away}`), `FB_BOOKS = ['unibet','betclic']` (winamax_foot permanently blocked since 11 juin anyway, and explicitly removed from the threshold check on 22 juin 2026 — `?.[key] ?? 0` handled its absence gracefully before that too). Edge computed via `removeVig(pair, 'h2h')` when all 3 odds present from the chosen bookmaker.
- Frontend sync: `syncFootballAlerts()` (`src/utils/syncAlerts.js`) merges into `fb_btts_alerts` / `fb_total_alerts` / `fb_result_alerts` localStorage keys, preserving accepted/rejected. `BTTSAlertCard`/`FootballTotalCard`/`FootballResultCard` (shared in `src/components/FootballAlertCards.jsx`, with `FB_LEAGUE_META`) render `pending` cards in `PlaceBetPage.jsx`.
- **Accepted → Running (compact format, like basketball)**: once accepted, the alert disappears from `PlaceBetPage` and `RunningPage.jsx`'s `footballAlertToGroup()` converts it into the same "group" shape as basketball props/totals (`stat: 'btts'|'total'|'result'`, `direction: 'yes'|'over'/'under'|'home'/'draw'/'away'`, `eventId: fixtureId`), fed into the shared `groupByMatch()` → rendered as a compact `MatchGroup` box (logo, league badge `CDM`/`L1`/`PL`/`Liga`/`BL`/`SA` via `FB_LEAGUE_LABEL`, team names, kickoff time, alert count) — no more dedicated "⚽ Football · Acceptés" card section. `AlertCard.goToMatch` navigates to `/football/${fixtureId}`. Live scores for CDM groups come from `/api/fd/worldcup` (matched by `String(g.id) === fixtureId.replace('fdcdm_','')`, crest `logo` URLs used directly by `TeamLogo`); the 5 European leagues have no live-score source yet so they stay in the "scheduled" bucket with initials only. `onDismiss` removes the alert from its localStorage key (`fb_btts_alerts`/`fb_total_alerts`/`fb_result_alerts`).
- **Settlement**: `resolveCompletedFootballAlerts()` (`src/utils/syncAlerts.js`, shared by `PlaceBetPage` and `RunningPage`) — generic `FOOTBALL_SETTLEMENT_SOURCES` array, one entry per fixtureId prefix, each with its own scores endpoint (`resolveFootballAlertResult()` computes won/lost identically regardless of source, from `{status, home:{score}, away:{score}}`). Covers **all** football leagues in the app as of 24 juillet 2026: `fdcdm_` (CDM, `/api/fd/worldcup`), `fdbr_` (Brasileirão, `/api/fd/bresil`), `afel_`/`afcl_`/`afch_` (coupes d'Europe, `/api/football/eucup/:comp/matches`), `fd_` (5 grands championnats, `/api/fd/results` — wraps the pre-existing backend-only `_getFdLeaguesResults()`, previously used only by `runAutoSettle()`/near-miss resolution and never exposed over HTTP for this frontend path, hence the earlier gap). `football_result` can only resolve at `STATUS_FINAL` (the 1X2 outcome can flip until the final whistle), unlike BTTS/O-U which can be "already won" live.
- **Fix 19 juin 2026 — cotes football figées au coup d'envoi (`_refreshOddsCache()`)**: avant ce fix, un match n'était marqué `frozen:true` que s'il *disparaissait complètement* du scrape Unibet/Betclic/Winamax — un match resté visible en live (bookmaker continue de renvoyer des cotes in-play) gardait ses cotes écrasées en continu, affichées comme si elles étaient pré-match (cas réel : Canada-Qatar, cotes 1X2 in-play 1.00/40/100 jamais figées). Fix : un match dont `commenceTime` est dépassé est désormais figé sur la dernière capture pré-coup d'envoi connue, peu importe si le bookmaker le renvoie encore en live. Effet de bord corrigé au passage : un match **futur** disparu temporairement du scrape (ligne pas encore postée) n'est plus marqué `frozen` à tort — il disparaît juste de la liste jusqu'à réapparition, au lieu d'afficher un faux badge "cotes pré-match".
- **Fenêtre d'alerte 48h — 5 grands championnats + Brasileirão (22 juillet 2026)**: `FOOTBALL_ALERT_WINDOW_MS = 48 * 3600_000` filtre désormais les fixtures des 5 ligues + Brasileirão à ≤48h avant génération d'alerte (jusque-là aucune fenêtre — un match dans 2 semaines pouvait déclencher une alerte sur des compos/forme qui allaient forcément changer d'ici là). La CDM garde sa fenêtre 24h séparée et préexistante, non touchée.

**api-football Pro — blessures, xG, snapshot (22 juillet 2026, `backend/server.js`):**
L'utilisateur a souscrit au plan Pro api-football (api-sports.io, même clé que `BASKETBALL_API_KEY`, variable `FOOTBALL_API_KEY` dans `.env` — le plan Free bloque toute donnée saison en cours). `footballApiFetch()`/`_footballApiFetchRaw()` — semaphore(5) + retry-on-rateLimit, même pattern que `bballFetch` (basket) — encapsule tous les appels api-football, capture le quota via `_captureFootballApiQuota()` (exposé dans `/api/system/health` → `quotas.footballApi`, affiché sur le Dashboard à côté d'API-Basketball).
- **Blessures** (`fetchFootballInjuriesForDate`, `computeFootballInjuryPenalties()`) — `/injuries?league=X&season=Y&date=Z` renvoie en un seul appel toutes les blessures/suspensions liées aux fixtures du jour. Un attaquant clé absent (`FB_KEY_ATTACKER_GOALS_ASSISTS=8` buts+passes) pénalise l'attaque (`FB_ATTACK_OUT_PENALTY=0.90`) ; un gardien titulaire absent (`FB_KEY_DEFENDER_APPEARANCES=10` apparitions) pénalise la défense (`FB_DEFENSE_OUT_PENALTY=1.12`). Pénalités multipliées dans `computeLambdas()` via 4 nouveaux paramètres optionnels (`homeAttackPenalty`/`homeDefensePenalty`/`awayAttackPenalty`/`awayDefensePenalty`, défaut 1 = no-op, rétrocompatible).
- **xG** (`fetchTeamRecentXG`, `tryGetFootballXGInputs`) — `/fixtures/statistics` (pas `/teams/statistics`, qui n'a que les buts bruts) sur les `FB_XG_RECENT_GAMES=8` derniers matchs (min `FB_XG_MIN_GAMES=3`), remplace les buts bruts (`homeGF`/`homeGA`) comme entrée de `computeLambdas` quand disponible — capture aussi `shotsPerGame`/`shotsOnTarget`/`possession` au passage, réutilisés pour remplir le panneau "Statistiques saison" de `MatchDetailPage.jsx` (`fetchXGStats`, route `GET /api/football/teamxgstats`).
- **Snapshot foot** (même principe que `_projectionsSnapshot` basket) — `_footballProjectionsSnapshot` persisté (`FB_SNAPSHOT_FILE`), écrit à chaque cycle `generateBackgroundAlerts()` juste après le calcul de `bttsProb`/`pOver25`/`pHome` etc., exposé via `GET /api/football/projections-snapshot/:fixtureId`. `MatchDetailPage.jsx` lit ce snapshot en priorité pour les 5 ligues + Brasileirão (non-CDM, qui a son propre calcul `computeCdmBTTS`) au lieu de recalculer indépendamment côté client avec une formule plus simple — supprime le badge "estimation site" sur ces compétitions, `cdmRho` s'applique aussi aux résultats snapshot (`bttsResult?.isSnapshot`).
- **Pas encore sur Render** : `FOOTBALL_API_KEY` seulement en local (`backend/.env`, gitignored) — décision explicite de l'utilisateur de différer l'ajout en production, tout dégrade proprement sans la clé (fallback buts bruts, pas de pénalité blessure, pas de snapshot).

**Coupes d'Europe — Ligue des Champions / Europa League / Conference League (23 juillet 2026, `backend/server.js`) :**
Ajoutées dans la Carte du Monde en parité complète avec les 5 championnats/CDM/Brasileirão : fixtures, cotes (Betclic+Unibet+Pinnacle), modèle Poisson (BTTS/O-U/1X2/Dixon-Coles), page match, alertes, règlement automatique, scores live, blessures.
- **Source de données : entièrement api-football** (football-data.org ne couvre pas ces compétitions). `EU_CLUB_COMP_IDS = { europa: 3, conference: 848, champions: 2 }` (ids Pinnacle distincts : 2632/271382/2627). `fixtureId` préfixés `afel_`/`afcl_`/`afch_`, namespace séparé de `fd_`/`fdcdm_`/`fdbr_`.
- **Forme d'équipe tous compétitions confondues** : `fetchClubRecentForm()`/`fetchClubRecentXG()` n'appliquent pas de filtre `league=` (contrairement aux 5 championnats domestiques) — les clubs en tour de qualification n'ont que 0-2 matchs dans la compétition elle-même. `avgGF`/`avgGA` calculés dynamiquement sur le pool courant puis figés par match via `freezeCdmPoolAvg()` (réutilisée telle quelle malgré son nom).
- **Blessures** : `/injuries` et `/teams` d'api-football acceptent ces 3 `competitionId` exactement comme un championnat domestique — `europa: 3, conference: 848, champions: 2` ajoutés directement dans `FOOTBALL_API_LEAGUE_IDS`, aucune logique séparée nécessaire, tout le pipeline `computeFootballInjuryPenalties()` existant s'applique tel quel.
- **Alertes coupées en tours de qualification, modèle actif quand même** (demande explicite : "on s'en fout des qualifs" ; affiné le 23 juillet 2026) : `fbFixtures` (loop `generateBackgroundAlerts()`) porte un flag `isQualifRound` (test `/qualif|preliminary|play-?offs?/i` sur `fx.league.round`) — les qualifs entrent quand même dans `computeLambdas`/near-miss tracking (`_logFootballNearMiss`, voir `/api/analysis/near-miss-football`), seule l'émission de l'alerte elle-même (`newAlerts.push`, les 5 points BTTS/Résultat/O-U/DC&BTTS/DC&Over + les 2 edges Pinnacle) est gardée par `!f.isQualifRound`. Se réactive automatiquement dès la phase finale (League Stage / élimination directe). `_resolveFootballNearMiss()` sait régler ces candidats (`FOOT_MATCH_ENDPOINT` étendu aux préfixes `afel_`/`afcl_`/`afch_` → `/api/football/eucup/:comp/matches`, même liste que `FOOTBALL_SETTLEMENT_SOURCES` côté frontend).
- `GET /api/football/eucup/:comp/matches` (`comp` ∈ `europa`/`conference`/`champions`) — liste de matchs générique, cache 30min (`_euClubMatchesCache`). `/api/football/teamxgstats` étend sa branche existante pour couvrir ces 3 compétitions (`resolveEuClubTeamId()` + `fetchClubRecentXG()`).
- **Limites de données connues (pas des bugs)** : pas de xG disponible au stade des qualifs (vérifié en direct, ex. Benfica) — le modèle retombe sur les buts bruts récents ; le panneau "Statistiques saison" reste à 0 pour ces 3 ligues (pas de classement saison côté api-football pour ces compétitions) ; Ligue des Champions sans page Betclic tant que leur 1er match (28 juillet) n'est pas ouvert côté bookmaker.
- **Fenêtre de scraping détaillé Betclic bornée à 72h** (`BETCLIC_EXTRAS_WINDOW_MS`) — évite de scraper le détail de matchs de qualif à plusieurs semaines, correctif qui bénéficie aussi aux 5 championnats existants. `fetchBetclicFootballExtras` a en plus un timeout dur de 15s (`Promise.race`) qui garantit la libération du sémaphore même si un match précis fait planter le scraping interne indéfiniment.
- **Fix 23 juillet 2026 — fuite mémoire réelle sur `_betclicGrpcMarketNames`/`_betclicGrpcCategory`** : le "timeout de lecture" interne (8s) appelait `resp.body.destroy()` — méthode `Readable` Node.js qui n'existe pas sur le `ReadableStream` Web Streams renvoyé par `fetch` natif (silencieusement avalée par le `try/catch` qui l'entourait, donc no-op). La requête + lecture du flux gRPC continuaient de tourner en arrière-plan indéfiniment si Betclic ne fermait jamais la connexion, bien après que le timeout dur de 15s de `fetchBetclicFootballExtras` ait abandonné côté appelant — connexions fantômes qui s'accumulent cycle après cycle (probable cause du mail Render "Web Service exceeded its memory limit" reçu le jour même). Fix : un seul `AbortController` par appel, `ctrl.abort()` dans le timeout coupe réellement la requête ET la lecture du flux. Repéré en observant qu'un cycle bg-alerts local restait bloqué (CPU ~0%, aucune progression du log) bien après la fenêtre de 15s censée le débloquer.
- **Règlement Brasileirão branché au passage** : en généralisant `resolveCompletedFootballAlerts()` (`FOOTBALL_SETTLEMENT_SOURCES` dans `syncAlerts.js`) pour couvrir ces 3 nouvelles ligues, découverte que le Brasileirão (scores dispo depuis le 17 juillet via `/api/fd/bresil`) n'avait en fait jamais été câblé au règlement — seule la CDM l'était. Corrigé dans la foulée.
- **Carte du Monde — CDM masquée** (`WorldMapPage.jsx`, `MONDE.leagues`) : la Coupe du Monde 2026 étant terminée (prochaine édition en 2030), `'cdm'` a été retiré du tableau `MONDE.leagues` — code/routes/settlement CDM intacts, juste plus affiché dans la navigation. Ordre des compétitions dans le panneau Monde : LDC → Europa → Conference → EuroLeague.

**MLB — supprimé du projet (27 août 2026)** : le marché Total runs (mode fantôme, jamais réellement alerté — `MLB_ALERTS_ENABLED` était resté `false` en dur depuis sa création) a été retiré entièrement sur demande explicite de l'utilisateur ("on va alleger tout ca") — `computeMlb.js`, toutes les routes/scrapers/caches `mlb_*` backend, `MlbDetailPage.jsx`, l'icône ⚾ Carte du Monde, le panneau MLB de Base de données, et le near-miss dédié. Aucune donnée financière perdue : `bet_ledger.json` ne contenait aucun pari MLB (jamais d'alerte réelle émise).

**Near-miss tracking — calibration des seuils d'alerte (`backend/server.js`) :**
Log, pour chaque candidat évalué par le modèle (qu'il déclenche une alerte réelle ou non), sa probabilité et le résultat final une fois le match/joueur réglé — sert à vérifier après coup si les seuils d'alerte (`RESULT_ALERT_PROB`, `TOTAL_ALERT_PROB`, `FB_BTTS_ALERT_PROB`…) sont bien placés, plutôt qu'au pif. 4 logs indépendants, tous résolus automatiquement toutes les 20min (même cadence que `generateBackgroundAlerts()`) :
- `GET /api/analysis/near-miss` — props joueurs NBA/WNBA (pts/reb/ast/3pts), le plus ancien des 4. Bande `[floor-20pts, floor)` uniquement (`NEAR_MISS_BAND = 0.20`) — le but est de juger si le seuil actuel est bien placé, pas de tracer toute la plage.
- `GET /api/analysis/near-miss-football` — BTTS/O-U/Résultat foot, même bande. Couvre aussi les tours de qualif des coupes d'Europe (alertes coupées mais modèle actif, voir plus haut).
- `GET /api/analysis/near-miss-basket-markets` (24 juillet 2026) — étend le principe à Résultat/Total O-U/Écart H2H basket, jusque-là aucun suivi sur ces 3 marchés (seuls les props l'avaient). Couvre NBA/WNBA/ACB/BBL/LegaA — **pas EuroLeague** (aucun modèle Résultat/Total/Écart pour elle). Écart H2H : point d'accroche différent des 2 autres, réutilise `stillAlt` déjà calculé par la boucle `findSpreadLadderAlternative(probFloor:0)` existante plutôt qu'un appel dédié. **Pas encore vérifié de bout en bout en direct** : aucune ligue n'avait de match dans la fenêtre "à venir" au moment du déploiement (NBA hors-saison, WNBA en pause All-Star jusqu'au 28 juillet, ACB/BBL/LegaA hors-saison l'été) — syntaxe et endpoint validés, logique de couverture spread vérifiée à la main sur papier, mais pas de cycle réel pending→won/lost observé. À contrôler dès le 28 juillet (retour WNBA).
- `GET /api/analysis/near-miss-mlb` (24 juillet 2026) — voir section MLB ci-dessus. Seul des 4 à logger **sans filtre de bande** (`MLB_ALERTS_ENABLED=false`, aucun seuil réel à défendre — la question est "le modèle est-il calibré du tout", pas la détection de cas limites).
- **How to apply — dans ~1 mois** : comparer `probability` vs `status` par tranche (et par `market` pour le near-miss basket). Si un seuil est mal placé sur un marché en particulier, l'ajuster avec un vrai chiffre à l'appui — même philosophie que le fix rebonds/tpm de juillet.

**ESPN abbreviation normalization (backend):**
ESPN uses short forms (`SA`, `NY`, `GS`, `NO`, `UT`) instead of standard NBA abbreviations. The backend normalizes bidirectionally:
```js
const ESPN_ABBR_MAP = { SAS:'SA', NYK:'NY', GSW:'GS', NOP:'NO', UTA:'UT' };
// Response keys are converted back: SA→SAS, NY→NYK, etc.
```

**Outrights — paris long terme vainqueur de compétition (23 juin 2026, `backend/server.js` + `src/pages/OutrightsPage.jsx`):**
7 compétitions (Ligue 1, PL, La Liga, Serie A, Bundesliga, NBA, WNBA), 3 sources bookmaker fusionnées. Betclic et PMU via Playwright (le marché "Vainqueur Compétition" n'est pas dans le HTML brut). Pinnacle via API JSON directe depuis le 26 juin 2026 (plus de Playwright pour Pinnacle) :
- **Betclic** (`fetchBetclicOutrights()`) — source principale, les 7 compétitions quand non bloqué. `BETCLIC_OUTRIGHT_TARGETS` (slug par compétition), gère bandeau cookies (peut réapparaître en différé — `_dismissCookieBanner()` appelé à plusieurs points), onglet "Compétition" optionnel, bouton "Afficher plus" (vérifie `.is-expanded` avant de cliquer pour ne pas re-fermer une liste déjà dépliée).
- **Pinnacle** (`fetchPinnacleOutrights()`) — via API JSON directe `guest.api.arcadia.pinnacle.com` (clé publique `CmX2KcMrXuFmNg6YFbmTxE0y9CIrOi0R`, interceptée du frontend Pinnacle). Plus de Playwright pour les outrights Pinnacle depuis le 26 juin 2026. **Étendu aux 5 championnats foot le 28 juillet 2026** (jusque-là NBA seule) — `PINNACLE_OUTRIGHT_LEAGUES` (id de ligue Pinnacle + regex du special "Winner"/"Champion" par compétition, vérifiés en direct : Ligue1=2036, PL=1980, Liga=2196, SerieA=2436, Bundesliga=1842, NBA=487 avec pattern `/nba champion/i` séparé). Boucle séquentielle avec délai 400ms entre ligues (une seule invocation, même cooldown 15min global).
- **PMU** (`fetchPmuOutrights()`) — PL, La Liga, Serie A, NBA, WNBA (Ligue 1 et Bundesliga pas disponibles chez eux pour le moment). Naviguer sur `www.pmu.fr` (pas `parisportif.pmu.fr`). Widget Kambi sous-jacent, nécessite un `waitForTimeout(3000)` après `goto` pour laisser le widget s'hydrater avant extraction.
- **Unibet abandonné pour cette feature** : la page outrights Unibet tronque à 3 favoris par défaut ; ni l'API Kambi (429 rate-limit confirmé) ni Playwright (signaux anti-bot type DataDome — modales cookies répétées, resets de session, 403) n'ont permis de débloquer la liste complète. Décision conjointe avec l'utilisateur d'abandonner Unibet et l'API Kambi entièrement pour les outrights — ne pas retenter sans nouvelle piste.
- **Garde-fous anti-ban** (suite à un vrai ban Betclic le 23 juin causé par des tests Playwright en rafale pendant le dev — voir [[project_betclic_ban_juin23]]) : `pinnacle`/`pmu` ajoutés aux objets partagés `_scraperBlockedUntil`/`_scraperFailStreak` (`scraper_blocks.json`, même mécanisme que `fetchBk`) ; `getOutrights()` vérifie le blocage Betclic **avant** de lancer Playwright (ne scrape jamais un bookmaker déjà bloqué) ; délai minimum de 15 min entre deux tentatives réelles par source, persisté dans `backend/cache/outright_attempts.json` (`_outrightAttempts`) pour survivre aux redémarrages `node --watch` pendant le dev ; auto-détection de blocage si moins de la moitié des compétitions réussissent dans un cycle.
- Fusion des 3 sources dans la route `GET /api/outrights` : `competitions[key].teams[].books = {betclic, pinnacle?, pmu?}`, réconciliation des noms d'équipe via alias maps (`PINNACLE_NBA_TEAM_ALIASES`, `PMU_TEAM_ALIASES`) car chaque bookmaker orthographie différemment (ex: "Inter" vs "Inter Milan", "FC Barcelone" vs "Barcelone").
- Frontend (`OutrightsPage.jsx`) : toggle sport ⚽/🏀, colonnes bookmaker dans un ordre fixe `BOOK_ORDER = ['pinnacle','betclic','pmu']` (Pinnacle blanc, Betclic rouge, PMU vert foncé `#166534`), compétitions triées par `COMP_ORDER` fixe (Ligue 1 en premier), une carte n'affiche que les colonnes bookmaker pour lesquelles elle a des données. **Fiche de compétition dépliable** (28 juillet 2026) : cliquer sur une équipe (`TeamRow` → `TeamDetailPanel`) fetch `GET /api/outrights/teamdetail?comp=X&team=Y` et affiche classement/écart/forme/calendrier à venir/blessures/historique/score composite/edge vs Pinnacle — consultable à tout moment, indépendamment d'une alerte.

**Alertes Outrights (28 juillet 2026, `backend/server.js` + `backend/computeOutrights.js`) :**
Décision explicite de l'utilisateur (conversation du 28 juillet) : contrairement aux props/résultats (calibrables sur des centaines de cas/mois), un outright ne se règle qu'une fois par saison — un simple edge mathématique vs Pinnacle ne suffit pas à juger un pick, il faut aussi des facteurs de fond. **2 types d'alerte indépendants**, tous deux filtrés par un **plancher de plausibilité partagé** (jamais d'alerte sur une équipe mathématiquement hors course, même très mal cotée) :
- `outright_model` — score composite 0-100 (`computeFootballOutrightScore`/`computeBasketballOutrightScore`, `computeOutrights.js`) : classement/pace (40pts), forme récente vs pace saison (25/30pts), attaque/défense ou net rating (20/30pts), calendrier restant (10/20pts), historique titres (5/10pts, table statique `OUTRIGHT_TROPHY_HISTORY`, approximative). Seuil `OUTRIGHT_MODEL_THRESHOLD = 70`.
- `outright_gap` — edge Pinnacle (vig-removed via `removeVigOutright()`, généralisation N-équipes de `removeVig`) vs Betclic/PMU. Seuil `OUTRIGHT_GAP_THRESHOLD = 0.15` (plus haut que le 2% du reste de l'app — vig-removal sur 15-20 équipes plus bruyant qu'un h2h à 2-3 issues).
- Plancher de plausibilité : foot = écart de points au leader ≤ 2× matchs restants (`computeFootballPlausibility`) ; basket = position playoff/play-in ou ≤6 matchs de la ligne (`computeBasketballPlausibility`, `OUTRIGHT_MAX_GAMES_BACK`). **Tous deux non calibrés (v1)**, même statut que `DIXON_COLES_RHO`/`SHRINK_K` — à revoir avec `near_miss_outrights.json` (log de tous les scores/edges évalués, alerte déclenchée ou pas) une fois une saison de recul accumulée.
- **Cadence 1x/jour** (`setInterval(generateOutrightAlerts, 24*3600_000)`), pas le cycle 20min — cotes outrights bougent lentement (`OUTRIGHTS_TTL` 3h), évite de sur-solliciter Betclic/PMU/Pinnacle.
- **Store dédié** (`_outrightAlerts`, `backend/cache/outright_alerts.json`) — volontairement **séparé** de `backgroundAlerts`/`_acceptedAlerts` (props/résultat/total) : ce store partagé purge par `eventId ∈ upcomingIds` (matchs à venir), un outright n'a pas de match/date à purger dessus, l'y mélanger l'aurait fait disparaître à chaque cycle. Routes `GET /api/outrights/alerts`, `POST /api/outrights/alerts/:id/{accept,reject,settle}`.
- **Réglement manuel uniquement** (`status: 'won'|'lost'` posé par l'utilisateur, bouton "Marquer gagné/perdu" dans `RunningPage.jsx` section "Outrights en cours") — pas de scraping fiable du vainqueur final de 7 compétitions sur plusieurs mois, décision actée avec l'utilisateur.
- **Frontend** : `syncOutrightAlerts()`/`acceptOutrightAlert()`/`rejectOutrightAlert()`/`settleOutrightAlert()`/`dismissOutrightAlert()` (`syncAlerts.js`) — contrairement aux autres types, le statut est déjà autoritaire côté backend (routes POST dédiées), pas besoin de la logique de préservation élaborée des autres syncs. Cartes `OutrightModelCard`/`OutrightGapCard` (`OutrightAlertCards.jsx`), rendues dans `PlaceBetPage.jsx` comme les autres types pending. `RunningPage.jsx` : section séparée "🏆 Outrights en cours" (pas `groupByMatch()`, aucun score live/date de match à grouper dessus).
- **Telegram** : `ALERT_TYPES.outright_model`/`outright_gap` (`telegram.js`). Accept/reject traité par une **branche dédiée et isolée** dans `/api/telegram/webhook` (server.js) — le store `_outrightAlerts` étant séparé de `backgroundAlerts`/`_acceptedAlerts`, la logique générique du webhook ne peut pas le résoudre ; retour anticipé avant le chemin générique pour ne jamais risquer de régression sur les autres types (argent réel en jeu).
- **Réutilisation, pas duplication** : `calcTeamNetRatingBg()` (extrait de `computeTeamWinProb()`, net rating d'UNE équipe) et `computeTeamAttackDefenseFactor()`/`shrinkFactor` (exportés de `computeFootball.js`) sont les MÊMES fonctions que celles utilisées pour les alertes Résultat/Total match-par-match — pas de 2e calculateur de force d'équipe divergent.
- **Bug de matching trouvé en testant** : "Paris SG" (PSG) et "Paris FC" (2 clubs distincts de Ligue 1) se faisaient confondre par le fuzzy-matching générique (`normTeam`/`fuzzy`, partagé avec le reste de l'app) — `OUTRIGHT_FD_NAME_ALIASES` + `findOutrightFdRow()` corrigent ce cas précis sans toucher au matching partagé.
- **Bug critique trouvé et corrigé — classement périmé (28 juillet 2026)** : football-data.org crée la nouvelle saison (`season.currentMatchday=1`, `season.winner=null`) **avant** de réinitialiser son tableau `standings` — PL/La Liga/Bundesliga renvoyaient encore la grille **finale de la saison précédente** (38/34 matchs joués) sous une saison techniquement déjà "nouvelle", produisant des alertes outrights sur Arsenal/Real Madrid/Man City/Nottingham Forest/Hamburger SV/Union Berlin comme si 2026/27 était presque jouée (Ligue 1/Serie A n'avaient pas ce problème, déjà à 0 joué). Détection : `_isFdStandingsStale(currentMatchday, table)` — si l'équipe en tête a joué plus de matchs que `currentMatchday` ne le permet, la table est celle d'une autre saison, ignorée entièrement (`_fdSeasonInfo[league.key].stale`, persisté `fd_season_info.json`) tant que FD ne l'a pas corrigée. Vérifié league par league via l'API FD brute avant de conclure (pas une supposition).
  - **Bug jumeau découvert en corrigeant celui-ci** : les 3 caches outrights (`_fdFullStandingsCache`/`_fdRemainingFixturesCache`/`_fdSeasonInfo`) n'étaient sauvegardés sur disque qu'**après la boucle complète des 5 ligues** dans `/api/fd/matches` — si FD renvoie un 429 en cours de route (quota 10 req/min très serré), les ligues déjà traitées avant l'interruption n'étaient jamais persistées, perdues au moindre redémarrage. Fix : sauvegarde à **chaque itération** de la boucle, pas seulement à la fin.
- **Audit Betclic vs PMU (31 juillet 2026, demande explicite) — construit puis retiré le jour même** : un audit hebdomadaire comparant les marchés Betclic/PMU par compétition + notif Telegram avait été construit, puis retiré sur retour utilisateur ("pas besoin de notif, si PMU ajoute un marché le scraper générique l'ajoute et c'est tout") — `_classifyPmuColumnTitle()`/`_classifyBetclicMarketTitle()` étant déjà génériques (pas une liste figée par marché), tout nouveau marché apparu chez un bookmaker est de toute façon capturé automatiquement au prochain scrape, sans code supplémentaire nécessaire. État constaté lors de l'investigation (31 juillet, toujours valable comme référence même sans le code d'audit) : PL manque Top4/5/6/10/Relégation côté PMU, La Liga et Ligue 1 manquent juste Relégation, Serie A/Bundesliga déjà alignés (Vainqueur seul des deux côtés). Pinnacle n'expose jamais de Top N/Relégation (confirmé le 30 juillet), donc rien à surveiller de ce côté non plus.
- **Limite connue (pas un bug)** : pénalité blessure pas encore câblée côté basket outrights (`injuryPenaltyPts: 0` en dur) — nécessiterait le roster complet + RotoWire par équipe contendante, différé volontairement (v1). Foot a sa pénalité (`getTeamCurrentInjuryPenaltyFootball`, réutilise `fetchFootballInjuriesForDate`/`fetchFootballPlayerSeasonStats`). Idem NBA (hors-saison) : pas de garde-fou explicite type "saison périmée" comme le foot, mais protégé de fait par l'absence de calendrier ESPN à venir (`teamschedule?upcoming=1` vide → `continue`) — à surveiller quand le calendrier 2026/27 apparaîtra sur ESPN.
- **Anti double-alerte sur cote dégradée + carte ladder (28 juillet 2026, demande explicite utilisateur)** : une fois un pick accepté à une cote donnée, plus aucune alerte (`outright_model` ou `outright_gap`, tous bookmakers confondus) sur la même équipe tant que la meilleure cote dispo ne dépasse pas celle acceptée (`_getBestAcceptedOutrightOdds()`). Si la cote s'améliore après coup, une **vraie nouvelle carte accept/reject** est créée (id dérivé `${id}_valueup${cote*100}`, `valueUpFrom` sur l'alerte) — mise supplémentaire optionnelle à décider soi-même, le pari déjà accepté n'est jamais modifié. Revu une 1ère fois en notif Telegram seule, changé sur retour utilisateur explicite (préfère une carte à un message informatif).
- **Distinction visuelle des 2 types** (revue le 28 juillet suite retour utilisateur) : texte entre parenthèses à côté du nom d'équipe ("Modèle perso" / "Écart de cote"), PAS la couleur — la bordure/dégradé code le **sport** (vert foot `#4ade80`, orange basket `#fb923c`, mêmes couleurs que le compte à rebours du Dashboard), pas le type d'alerte.
- **Badge sidebar** : compteur `outright` ajouté à `useAlertCount()` (App.jsx, clé localStorage `outright_alerts`, pas de fixtureDate à vérifier contrairement aux autres — juste le statut). Affiché sur la ligne "Alertes" (à droite du badge existant) ET sur l'item "Outrights" du nav.

**Alertes sur les marchés secondaires — Top N / Relégation / Conférence NBA (2 août 2026, `generateOutrightCutoffAlerts()` dans `backend/server.js`) :** étend les alertes `outright_model`/`outright_gap` (jusque-là "Vainqueur" seul) aux marchés déjà scrapés/affichés (voir section suivante) mais jamais alertés. Principe : `computeFootballOutrightScore`/`computeBasketballOutrightScore` (`computeOutrights.js`) sont déjà génériques (`leaderPace`/`leaderWinPct` sont de simples paramètres) — pour "Top N", on leur passe le rythme/bilan de l'équipe qui occupe *aujourd'hui* la position N au lieu du vrai leader, **sans aucune nouvelle fonction**. Seule la Relégation a besoin d'un sens réellement inversé (`computeFootballRelegationScore`, nouvelle fonction) : rythme lent, forme qui empire, attaque/défense faible, calendrier dur = bon score (au lieu du contraire) ; l'historique de titres (5pts) n'a pas de sens ici, redistribué au calendrier (10→15pts).
- **Fonction séparée de `generateOutrightAlerts()`**, avec son propre `seenIds`/purge scopée au champ `market` — zéro ligne touchée dans la fonction Vainqueur existante. Seuils plus stricts : score ≥85 (vs 70 pour Vainqueur), marge de plausibilité ×1,2 (vs ×2) — nécessaire car plusieurs équipes peuvent avoir un excellent score "Top N" simultanément (contrairement à "Vainqueur", un seul leader possible), sans resserrement ça inonderait d'alertes triviales.
- **Jamais de comparaison Pinnacle (`outright_gap`) sauf Conférence NBA** — vérifié en direct que Pinnacle n'expose structurellement aucune cote Top N/Relégation pour aucune ligue.
- **Marchés construits** : Ligue 1 (Top 3, Relégation), Premier League (Top 2/4/5/6/10, Relégation), La Liga (Top 4, Relégation), NBA (Conférence Est/Ouest, **Vainqueur de division** — 6 divisions, ajouté 2 août 2026). Serie A/Bundesliga exclues (aucun marché secondaire chez aucun bookmaker). **WNBA "Finaliste" construit le 2 août 2026** (voir ci-dessous) ; **reporté** : NBA "Fait les playoffs" (Oui/Non) — forme de cote différente (binaire par équipe, `_buildMergedTeamProps`) demandant un traitement de carte à part non détaillé au moment de construire le reste.
- **Vainqueur de division NBA + Finaliste WNBA (2 août 2026)** : mêmes principes que Top N/Relégation, sans nouvelle formule. Divisions NBA : `NBA_DIVISION_TEAMS` (table statique des 6×5 équipes — ESPN ne renvoie que le groupement par conférence, pas de sous-groupe division dans `/apis/v2/sports/basketball/nba/standings`, vérifié en direct), comparé au leader de la division au lieu du leader de conférence/ligue. Finaliste WNBA : traité comme un "Top 2" sur le classement unique de la ligue (plus de vraies conférences WNBA depuis 2022), équipe-charnière = 2e place du classement. Nouveau helper partagé `gamesBackFrom(refRow, row)` (écart en "matchs derrière" façon classement US) pour le plancher de plausibilité des deux. Seuil division NBA = 70 (comme Conférence, un seul leader plausible par groupe de 5) ; seuil finaliste WNBA = 85 (comme Top N foot, plusieurs équipes peuvent viser les 2 dernières places en même temps). **Aucun `outright_gap`** sur ces deux marchés — Betclic seul, ni PMU ni Pinnacle ne les exposent (vérifié en direct) ; l'edge s'activera automatiquement si un bookmaker en ajoute un jour, sans changement de code. Vérifié en direct : Finaliste WNBA loggue bien des candidats near-miss (6 équipes plausibles, scores 66.9-81.3, tous sous le seuil 85 → pas encore d'alerte, comportement attendu) ; Division NBA correctement gelée par le garde-fou saison-pas-commencée ci-dessous (même gate que Conférence), untestable avant la reprise 2026-27.
- **Bug trouvé et corrigé le 2 août — cotes Pinnacle absentes sur la fiche compétition** : `_outrightMarketsCache.pinnacle[key]` (lu par `GET /api/outrights/:compKey/markets`, page détail par compétition) n'était écrit que pour les compétitions ayant un marché Pinnacle secondaire (seule la Conférence NBA) — Ligue 1/PL/La Liga/Serie A/Bundesliga en étaient donc privées sur cette page précise, alors que la cote Vainqueur Pinnacle existait déjà et s'affichait normalement sur la page Outrights principale (source différente, `_pinnacleOutrightsCache`). Fix dans `fetchPinnacleOutrights()` : `{winner: teams, ...extras}` est désormais toujours écrit dans ce cache, peu importe si des marchés additionnels existent.
- **Limite pré-existante découverte, 3 causes toutes corrigées** : le système Vainqueur NBA ne produisait aucune entrée near-miss (0 de tout temps). (1) "Philadelphie 76ers"/"Los Angeles Clippers" (Betclic) ne matchaient jamais "Philadelphia 76ers"/"LA Clippers" (ESPN) via `fuzzy()` — nouvelle table `NBA_ESPN_TEAM_ALIASES` + `fuzzyNbaEspn()`, scopée à ce point de comparaison. (2) **Cause racine réelle** : hors-saison, ESPN continue de renvoyer les vrais chiffres de fin de saison précédente (ex. Detroit 60-22) au lieu de zéros, tout en taguant déjà la réponse avec la vraie date de reprise (`season.startDate`) — `/api/nba/teamschedule/:id?upcoming=1` renvoyant 0 match de la saison en cours n'était qu'une conséquence en cascade, pas la cause. Fix (même principe que `_isFdStandingsStale` foot / `isEuroBasketSeasonCurrent` basket EU) : `/api/nba/standings` et `/api/nba/leaders` neutralisent leurs stats (identité d'équipe conservée, wins/losses/pct/gb à 0, rank à 1 ; leaders entièrement vides) tant que `season.startDate` est dans le futur — `generateOutrightAlerts()`/`generateOutrightCutoffAlerts()` sautent alors proprement le traitement NBA (log explicite) plutôt que de scorer sur un classement obsolète. Étendu sur demande explicite à l'affichage Carte du Monde (mêmes routes, donc aucun changement frontend nécessaire). Se réactive automatiquement à la vraie reprise de saison, sans changement de code.
- Frontend : `MARKET_LABELS`/`marketLabel()` extraits de `OutrightCompetitionPage.jsx` vers `src/utils/outrightMarkets.js` (partagé), `OutrightAlertCards.jsx` affiche le marché sur la carte ("Modèle perso · Top 4") via `alert.market`.

**Marchés Top N / Podium / Relégation par compétition (28 juillet 2026, `backend/server.js` + `src/pages/OutrightCompetitionPage.jsx`) :**
Demande explicite de l'utilisateur : voir, pour chaque championnat foot, les marchés de qualification (Top 2/4/5/6/10 selon coupe d'Europe visée) et de maintien en plus du simple "Vainqueur". Additif — `/api/outrights` (route existante, marché Vainqueur seul) et les alertes `outright_model`/`outright_gap` ne sont pas touchés, zéro risque de régression sur ce qui tourne déjà en prod.
- **Vérifié en direct avant de construire** : chaque championnat a un jeu de marchés différent selon le bookmaker — pas de liste figée "Top 2/4/6/10 partout". Betclic Premier League est le cas le plus riche (`winner`/`top2`/`top4`/`top5`/`top6`/`top10`/`relegation`), La Liga a `winner`+`top4`, Serie A/Bundesliga/NBA/WNBA n'ont que `winner`. Le scraper est donc **générique par ligue** (parcourt tous les `.marketBox`/colonnes présents plutôt qu'une liste attendue), pas une liste de marchés imposée.
- **Betclic** (`_scrapeBetclicOutrightPage()`, réécrit) — parcourt tous les `.marketBox` de la page déjà chargée (zéro appel réseau en plus par rapport à avant) et classe chaque titre via `_classifyBetclicMarketTitle()` (regex `/^Vainqueur/`→`winner`, `/^Podium/`→`top3`, `/^Plac[ée]\s*1-(\d+)/`→`top${N}`, `/^Rel[ée]gation/`→`relegation`). `fetchBetclicOutrights()` garde `teams` (Vainqueur seul, rétrocompat avec `_buildMergedCompetitions`/`generateOutrightAlerts`) et ajoute `markets` (tous les marchés) dans le même retour.
- **PMU** (`_scrapePmuOutrightPage()`, réécrit) — le widget "Classement" a plusieurs colonnes d'odds par ligne (`.KambiBC-outcome-button` ×N) précédées d'en-têtes (`.KambiBC-sports-hub-bet-offers-view__outcome-title`, ex. "Vainqueur"/"Top 3") lus une fois avant la boucle des lignes, classés via `_classifyPmuColumnTitle()` (même principe que Betclic).
  - **Bug trouvé et corrigé, vérifié de bout en bout en direct — sous-onglet "Buteurs" par défaut sur Premier League** : la page PMU Premier League a un menu de sous-onglets propre à cette ligue (`.KambiBC-filter-menu__option` : "Buteurs" / "Équipes A-Z" / "Compétition"), absent des autres championnats, et atterrit par défaut sur "Buteurs" (marché meilleur buteur, joueurs) plutôt que "Compétition" (marché vainqueur équipe) — le scraper cherchait un conteneur matchant `text=/Vainqueur/i` sans jamais cliquer ce sous-onglet, tombait donc sur le widget buteurs (même titre interne "Vainqueur", générique et réutilisé pour les deux marchés selon l'onglet actif). **C'était la cause racine du bug signalé par l'utilisateur "plus de cote PMU sur Premier League"** (le nom du joueur ne matchait jamais rien côté `normPmuTeam`, donc la colonne PMU restait vide). 1er correctif tenté (`.filter({ hasNotText: /buteur/i })` sur le conteneur) **raté** — confirmé par un vrai scrape (25 entrées, 100% joueurs) : le mot "buteur" n'apparaît que dans le libellé du sous-onglet, jamais dans le conteneur du marché. Vrai fix : clic explicite sur le sous-onglet "Compétition" avant la recherche du conteneur (no-op si absent). Vérifié par un nouveau vrai scrape : PMU renvoie maintenant les cotes vainqueur pour les 7 compétitions, colonne PMU réapparue sur Premier League avec des cotes cohérentes vs Betclic/Pinnacle. Bonus découvert au passage : PMU Premier League a aussi un marché Top 2.
- **Cache dédié** `_outrightMarketsCache = {betclic:{}, pmu:{}}` (persisté `backend/cache/outright_markets.json`) — séparé de `_outrightsCache`/`_pmuOutrightsCache` (tableaux plats "vainqueur seul" qui ne peuvent pas porter de propriété custom après `JSON.stringify`).
- **Fusion** `_buildMergedCompetitionMarkets(betclicMarkets, pmuMarkets)` — proche de `_buildMergedCompetitions` mais indexée par marché. Pas de Pinnacle ici (n'expose que Vainqueur).
- **Route** `GET /api/outrights/:compKey/markets` — lit `_outrightMarketsCache` (alimenté en side-effect par les scrapers existants), n'appelle `getOutrights()`/`getPmuOutrights()` que pour respecter leur propre TTL 3h / anti-ban, ne force jamais de scrape supplémentaire par la simple consultation.
- **Frontend** : titre de chaque `Card` sur `/outrights` cliquable → `/outrights/:compKey` (`OutrightCompetitionPage.jsx`, nouvelle page). Onglets = un par marché réellement disponible pour cette compétition (`MARKET_ORDER`/`MARKET_LABELS`, filet `Top ${N}` générique pour un palier non explicitement listé). `Card`/`BOOK_ORDER`/`BOOK_LABELS`/`BOOK_ODDS_COLORS`/`BOOK_COL_WIDTH` exportés depuis `OutrightsPage.jsx` et réutilisés plutôt que dupliqués. Pas de fiche dépliable (`TeamDetailPanel`) sur cette page — le score composite qu'elle affiche évalue la chance de gagner la compétition, pas d'atteindre un Top N, l'y montrer serait trompeur.

---

## Basketball — BasketballPage.jsx

The NBA list comes from the **live ESPN scoreboard** (`/api/nba/scoreboard`), not static fixtures.

**Display rules:**
- Matchs triés par date **croissante** (plus ancien → plus récent)
- Matchs terminés affichés **48h max** après leur date, puis filtrés automatiquement
- Euroleague : fixtures statiques depuis `basketball.js`, mêmes règles de tri et rétention

---

## Basketball — BasketballDetailPage.jsx

### ESPN_NBA map (name → ESPN teamId)
Used to fetch rosters via `/api/nba/players/:teamId`. The `/roster` endpoint uses different IDs than `/teams` — always use these verified IDs:

```js
const ESPN_NBA = {
  'Atlanta Hawks': 1,        'Boston Celtics': 2,       'New Orleans Pelicans': 3,
  'Chicago Bulls': 4,        'Cleveland Cavaliers': 5,  'Dallas Mavericks': 6,
  'Denver Nuggets': 7,       'Detroit Pistons': 8,      'Golden State Warriors': 9,
  'Houston Rockets': 10,     'Indiana Pacers': 11,      'LA Clippers': 12,
  'Los Angeles Lakers': 13,  'Miami Heat': 14,          'Milwaukee Bucks': 15,
  'Minnesota Timberwolves': 16, 'Brooklyn Nets': 17,
  'New York Knicks': 18,     'Orlando Magic': 19,       'Philadelphia 76ers': 20,
  'Phoenix Suns': 21,        'Portland Trail Blazers': 22, 'Sacramento Kings': 23,
  'San Antonio Spurs': 24,   'Oklahoma City Thunder': 25,  'Utah Jazz': 26,
  'Washington Wizards': 27,  'Toronto Raptors': 28,     'Memphis Grizzlies': 29,
  'Charlotte Hornets': 30,
};
```

### ESPN abbreviation normalization (frontend)
ESPN's live scoreboard returns short abbreviations (`SA`, `NY`, `GS`…). When looking up boxscore keys, always normalize:
```js
const ESPN_SHORT = { SA: 'SAS', NY: 'NYK', GS: 'GSW', NO: 'NOP', UT: 'UTA' };
const normalizeAbbr = a => ESPN_SHORT[a?.toUpperCase()] || a?.toUpperCase() || '';
```
This is critical for matching the `team.short` from a live fixture against the boxscore response keys.

### isCompleted detection
```js
const isCompleted = fixture.round?.includes('Terminé') || new Date(fixture.date) < new Date();
```
Note: `'Terminée'.includes('Terminé')` = true — both forms work.

### Section "Analyse Props" — PropsSection
Single unified component (no longer split into two). Opens when the user clicks the bar-chart icon in the info bar. **Cliquer sur le titre "Analyse Props" ferme la section.**

**Layout:** grid à 9 colonnes — `26px 1fr 36px 36px 36px 8px 36px 36px 36px`
- Colonne 1–5 : Position | Nom | **Projeté** Pts / Rebs / Asst
- Colonne 6 : séparateur vertical
- Colonne 7–9 : **Réalisé** Pts / Rebs / Asst (vert si `isCompleted`, `—` sinon)

**Toggle équipe** : boutons `home.short` / `away.short` en haut à droite — switchent les deux colonnes ensemble.

**Roster** : joueurs triés par pts saison décroissant, séparateur `—— Remplaçants ——` entre les 5 premiers (titulaires) et le reste.

**Matching joueur** (projected ↔ realized) :
```js
realized.find(r =>
  String(r.id) === String(p.id) ||        // ESPN athlete ID (string comparison)
  r.name === p.name ||                     // nom exact
  lastName(r.name) === lastName(p.name)   // nom de famille en fallback
)
```

**Box score fetch** : déclenché au mount de PropsSection si `isCompleted`. Cache 5min côté backend.

---

## Basketball — basketball.js

### NBA Playoffs 2026 — état au 18 juin 2026 (saison terminée, NYK Champion)

| ID | Match | Statut |
|---|---|---|
| b001 | OKC (1W, 64-18) vs SAS (2W, 62-20) | Finales Conf. Ouest – **Terminée G1** (SAS 122-115, SAS 1-0) |
| b011 | OKC vs SAS | Finales Conf. Ouest – **Terminée G2** (OKC 122-113, Série 1-1) |
| b012 | SAS (home) vs OKC | Finales Conf. Ouest – **Terminée G3** (OKC 123-108, OKC 2-1) |
| b002 | DET (1E, 60-22) vs CLE (4E, 52-30) | Demi-Finales Conf. Est – **Terminée** CLE 4-3 (G7 le 18 mai, CLE 125-94) |
| b003 | PHI (2E, 55-27) vs NYK (3E, 53-29) | Demi-Finales Conf. Est – **Terminée** NYK 4-0 |
| b004 | MIN (6W, 49-33) vs SAS (2W, 62-20) | Demi-Finales Conf. Ouest – **Terminée** SAS 4-2 (G6 le 15 mai, SAS 139-109) |
| b005 | LAL (4W, 53-29) vs OKC (1W, 64-18) | Demi-Finales Conf. Ouest – **Terminée** OKC 4-0 |
| b008 | OLY (home) vs FEN | Euroleague – **Terminée Demi A** (OLY 79-61, EL game 404, Athènes) |
| b009 | VBC (home) vs RMB | Euroleague – **Terminée Demi B** (RMB 105-90, EL game 405, Athènes) |
| b010 | NYK vs CLE | Finales Conf. Est – **Terminée G1** (NYK 115-104 OT, NYK 1-0) |
| b013 | NYK vs CLE | Finales Conf. Est – **Terminée G2** (NYK 109-93, NYK 2-0) |
| b014 | CLE (home) vs NYK | Finales Conf. Est – **Terminée G3** (NYK 121-108, NYK 3-0) |
| b015 | SAS (home) vs OKC | Finales Conf. Ouest – **Terminée G4** (SAS 103-82, Série 2-2) |
| b016 | — | Euroleague – **3e place non jouée** (l'EL n'organise pas de match pour la 3e place) |
| b017 | OLY (home) vs RMB | Euroleague – **Terminée Finale** (OLY 92–85 RMB, **OLY Champion**, EL game 406, Athènes) |
| b018 | CLE (home) vs NYK | Finales Conf. Est – **Game 4** (25 mai, 20h ET, Rocket Mortgage FieldHouse, NYK 3-0) |
| b019 | OKC (home) vs SAS | Finales Conf. Ouest – **Terminée G5** (OKC 127-114, OKC 3-2) |
| b020 | SAS (home) vs OKC | Finales Conf. Ouest – **Terminée G6** (SAS 118-91, Série 3-3) |
| b021 | OKC (home) vs SAS | Finales Conf. Ouest – **Terminée G7** (SAS 111-103, SAS 4-3) |
| b018 | CLE (home) vs NYK | Finales Conf. Est – **Terminée G4** (NYK 130-93, NYK 4-0) |
| b022 | SAS (home) vs NYK | Finales NBA – **Terminée G1** (NYK 105-95, NYK 1-0) |
| b023 | SAS (home) vs NYK | Finales NBA – **Terminée G2** (NYK 105-104, NYK 2-0) |
| b024 | NYK (home) vs SAS | Finales NBA – **Terminée G3** (SAS 115-111, NYK 2-1) |
| b025 | NYK (home) vs SAS | Finales NBA – **Terminée G4** (NYK 107-106, NYK 3-1) |
| b026 | SAS (home) vs NYK | Finales NBA – **Terminée G5** (NYK 94-90, **NYK CHAMPION 2026**, série 4-1) |

**Note EL :** Venue Final Four 2026 = TELEKOM CENTER ATHENS, Marousi (18 500 places). Scores patchés automatiquement via `/api/euroleague/scoreboard` + `useFixtures.js`. b006/b007 supprimés — PAO éliminé par VBC (3-2).

**Règle :** toujours utiliser des vraies stats (StatMuse, ESPN, CBS Sports). Ne jamais inventer. Vérifier le bracket actuel sur StatMuse avant d'éditer.

### Stats équipes clés (saison régulière 2025-26)

| Équipe | Bilan | PPG | OPPG | RPG | APG | FG% |
|---|---|---|---|---|---|---|
| OKC | 64-18 | 119.0 | 108.2 | 44.1 | 27.5 | 48.4 |
| SAS | 62-20 | 119.8 | 109.4 | 47.0 | 28.1 | 47.8 |
| DET | 60-22 | 116.8 | 110.4 | 45.2 | 26.4 | 47.2 |
| NYK | 53-29 | 116.5 | 112.6 | 45.6 | 27.4 | 47.1 |
| CLE | 52-30 | 119.5 | 113.8 | 45.4 | 27.2 | 47.4 |
| LAL | 53-29 | 117.8 | 114.2 | 44.6 | 27.1 | 47.2 |
| MIN | 49-33 | 118.0 | 114.6 | 44.1 | 26.1 | 46.8 |
| PHI | 55-27 | 116.4 | 113.2 | 44.1 | 26.2 | 47.2 |

---

## Environment variables (`backend/.env`)

| Variable | Source | Free tier |
|---|---|---|
| `FD_API_KEY` | football-data.org | 10 req/min |
| `BASKETBALL_API_KEY` | api-sports.io (basketball, plan Pro) | 7500 req/day |
| `FOOTBALL_API_KEY` | api-sports.io (football, plan Pro — 22 juillet 2026) | 7500 req/day, 300 req/min |
| `ODDS_API_KEY` | the-odds-api.com | 500 req/month |
| `BZZOIRO_API_KEY` | sports.bzzoiro.com | — (EuroLeague uniquement, voir ci-dessous) |
| `VALUE_THRESHOLD` | — | default `2` (%) |
| `PORT` | — | default `3001` |

All API keys are optional — backend returns 503 for routes whose key is missing.

## Basketball EU leagues — data sources (ACB / LNB / BBL / Lega A)

Migration 22 juin 2026 : **Bzzoiro coupé pour ces 4 ligues** (instable, jugé peu fiable) — roster, gamelog et box-score reposent désormais sur `BBALL_BASE = v1.basketball.api-sports.io` (`bballFetch`/`bballPlayerGamelog`/`getEuroGamesMap` in `server.js`). Bzzoiro (`bzzFetch`) reste utilisé **uniquement pour l'EuroLeague** (`BZZ_EL_TEAMS`, routes `/api/euroleague/*`) — ne pas y toucher en travaillant sur les 4 ligues EU.

- **ACB** : roster + gamelog principal restent sur scraping direct acb.com (`fetchAcbHtml`/`ACB_TEAM_MAP`) — plus complet qu'api-sports.io (steals/blocks/turnovers inclus). api-sports.io ne couvre que ce qui manquait côté ACB (compos, box-score auto-save).
- **LNB / BBL / Lega A** : roster + gamelog + box-score entièrement via api-sports.io.
- **Lega A** : compositions confirmées restent sur le scraping officiel legabasket.it (`fetchLegaALineup`), inchangé par cette migration.
- **Important — pas de lineups pré-match natif côté api-sports.io basketball** (`/lineups?game=` renvoie `"This endpoint do not exist."` — vérifié en direct le 22 juin 2026). Les "compos probables" ACB/LNB/BBL sont calculées depuis l'historique de titularisations (`type: 'starters'`) des gamelogs récents (`bballPlayerGamelog`), pas depuis un lineup pré-match confirmé. Seule la Lega A a une vraie source de compos confirmées pré-match (legabasket.it).
- **Concurrence api-sports.io** : un roster complet déclenche un appel `/games/statistics/players?player=X` par joueur (~15-19 en parallèle) — sans throttling ça déclenche un rate-limit (réponse HTTP 200 avec `errors.rateLimit`, pas une erreur HTTP, donc invisible si non détectée). `bballFetch` limite à 5 appels simultanés (semaphore) + retry avec backoff sur `rateLimit`, et `getEuroGamesMap` déduplique les appels concurrents identiques.
- **Fix 28 juillet 2026 — quota vidé hors-saison par `getEuroDefByPos()`** : cette fonction (défense adverse par poste, reb/ast/tpm — voir "Alertes basket EU" plus bas) était appelée en tête de boucle dans `runEUPropsAlerts()`, **avant** de vérifier s'il y avait le moindre match programmé pour la ligue. À froid (cache 6h expiré), elle parcourt tout le pool de la ligue (`/teams` + `/players` par équipe + `bballPlayerGamelog` par joueur — ~800-900 appels/ligue). Comme `_euroCache` n'est jamais persisté sur disque, chaque redémarrage backend (fréquent en dev avec `node --watch`) le remettait à zéro, donc le prochain cycle bg-alerts refaisait ce calcul complet même en plein hors-saison (ACB/BBL/Lega A à 0 match programmé en juillet), pour un résultat jamais utilisé. Fix : le calcul est maintenant fait **après** la vérification `upcoming.length` — sauté entièrement s'il n'y a aucun match à venir dans la ligue. Comportement identique quand il y a un match ; aucune autre section EU (Résultat/Écart H2H/Total O/U) n'avait ce problème, elles étaient déjà toutes gated sur les matchs programmés.

---

## Session 31 juillet 2026 — widget "5 prochains matchs" + fiabilité Telegram Écart H2H

**Widget "5 prochains matchs" (`MatchDetailPage.jsx`) refondu en ligne unique avec bascule** — remplace l'ancien layout 2 colonnes (domicile/extérieur côte à côte, format liste détaillée date/lieu/nom déjà simplifié en logos le même jour). Nouveau format, sur demande explicite utilisateur : une seule ligne — titre "5 prochains matchs" + logo de l'équipe active à gauche (à côté du mot, pas en liste séparée), les 5 vrais logos adverses (crests réels via `oppCrest` de l'API football-data.org, pas des initiales placeholder) + un petit bouton `⇄` à droite. Le bouton bascule entre les 5 prochains matchs du domicile et de l'extérieur avec la **même animation flip que le toggle Pinnacle du Backtesting** (collapse horizontal `scaleX(0)`+fade 280ms, pas de `@keyframes` CSS — purement `transition`+`transform` inline + `setTimeout` calé sur la même durée). État : `upcomingSide`/`upcomingFlipping`/`handleUpcomingToggle`. Tailles finales après plusieurs allers-retours utilisateur : logos adverses 15px (espacés `gap:1.1rem`), bouton toggle 12px (volontairement plus petit que les logos, fond bleu `rgba(96,165,250,...)` contrasté pour rester visible malgré la taille). `CollapsibleCard` abandonné pour ce widget (plus rien à replier — tout tient sur la ligne d'en-tête) au profit d'une `<section className="detail-card upcoming-card">` avec un `upcoming-header-row` en flex `justify-content:space-between`.
- **Bug trouvé en ajustant l'espacement haut/bas** : `.card-title` porte un `margin-bottom:0.65rem` pensé pour les titres empilés au-dessus d'un contenu (cas normal des autres cards) — réutilisé tel quel ici où le titre est en ligne avec le reste, ce margin ajoutait ~10px sous le texte sans équivalent au-dessus, rendant la card visuellement asymétrique (top gap 15px / bottom gap 26px, mesuré via Playwright `boundingBox()`). Fix : `.upcoming-title-with-logo { margin-bottom: 0 }` + `.upcoming-card { padding: 0.65rem 1rem }` — symétrique à 14.6px des deux côtés, hauteur de card réduite de 57px à 45px.
- CSS mortes supprimées au passage (`.upcoming-grid`/`.upcoming-col`/`.upcoming-team-header`/`.upcoming-row`, plus référencées par aucun JSX depuis la refonte).

**Fiabilité notifications Telegram — Écart H2H (`generateBackgroundAlerts()`, `backend/server.js`) :**
Cas réel signalé par l'utilisateur : notif Telegram reçue pour `401857099_wnba_spread_away` (Minnesota Lynx -10,5), mais alerte absente de l'app à l'ouverture. Diagnostic confirmé via `telegram_notified.json` (id bien notifié) vs `backgroundAlerts` en mémoire (id absent) : l'alerte avait qualifié à un cycle, puis **le cycle suivant (~20 min après)** ne l'a plus retrouvée valable (cote qui bouge / plus d'alternative sur l'échelle `findSpreadLadderAlternative`) — `refreshOrDropPendingById` retire alors silencieusement toute alerte **encore pending** (jamais une alerte déjà acceptée, cf. code) dès qu'elle ne re-qualifie plus, sans laisser de trace ni avertissement. Comportement volontaire et déjà en place (pas un bug introduit cette session) — mais laissait le message Telegram tel quel, comme si le pari restait jouable.
- **Fix 1 — message Telegram édité à l'expiration** : dans la boucle de purge `for (const staleId of _staleAlertIds)`, si l'alerte purgée est de type `basketball_spread`, le message Telegram déjà envoyé (retrouvé via `_debugTokensForId(staleId)[0]?.messageId`) est édité (`editTelegramMessage`) pour ajouter "⚠️ Expirée — ligne plus valable" sous le texte original. Scopé à `basketball_spread` uniquement — seul marché où le cas a été observé jusqu'ici, pas une règle générale automatiquement étendue aux autres types. Notification initiale conservée (une 1ère version de ce fix désactivait carrément l'envoi Telegram pour ce marché — rejetée par l'utilisateur : "il faut laisser les notifs... juste faire un truc pour pas avoir la notif [si] obsolete").
- **Fix 2 — ordre app-avant-Telegram** : `generateBackgroundAlerts()` attendait la fin de l'envoi Telegram (`await notifyNewAlert(...)`, aller-retour réseau vers l'API Telegram) **avant** de publier `backgroundAlerts = Object.values(byId)` et le signal SSE — l'app pouvait donc rester en retard sur la notif Telegram le temps de cet aller-retour. Réordonné : `backgroundAlerts`/signal SSE publiés en premier, envoi Telegram juste après. L'app n'est plus jamais plus lente que la notif à recevoir une alerte fraîchement créée (n'a pas d'effet sur la disparition documentée ci-dessus, qui reste liée à l'écart de ~20 min entre deux cycles, pas à cet ordre).
- **À évaluer plus tard** : utilisateur a explicitement dit vouloir revoir après coup si le message "Expirée" apporte quelque chose d'utile en pratique.

**Carte du Monde — tous les championnats dépliés par défaut + repli par jour (`WorldMapPage.jsx`, `Panel`) :**
Depuis le 24 juillet 2026, tous les championnats étaient repliés par défaut à l'ouverture d'un pays (`openLeagues` initialisé à `{}`) — il fallait un clic pour voir les matchs. **Revenu en arrière le 31 juillet 2026** (demande explicite, affinée en plusieurs étapes) : `openLeagues` s'initialise maintenant avec **tous** les championnats du pays à `true` (`_allLeaguesOpen = Object.fromEntries(country.leagues.map(l => [l, true]))`), donc la liste de matchs s'affiche directement dépliée pour n'importe quel pays (Espagne/La Liga+ACB, USA/NBA+WNBA, etc.), pas seulement les pays à un seul championnat comme au 1er passage. Le `useEffect` qui réinitialise déjà `sportFilter` au changement de pays (le `Panel` ne remonte pas quand on change de pays sans le fermer) réinitialise `openLeagues`/`openDays` de la même façon. **Ne touche pas `statsLeague`** (overlay plein écran Classement/leaders basket) qui reste gated sur un clic explicite sur le championnat — un overlay qui s'ouvrirait tout seul serait trop intrusif, seule la liste de matchs se déplie automatiquement. Vérifié en direct sur Espagne (foot+basket) et USA (NBA+WNBA) : tout s'affiche direct, aucun overlay parasite.
- **Repli par jour compense la densité** (ajouté dans la même session, avant l'extension à tous les pays) : dans la liste de matchs d'un championnat déplié, seul le jour le plus proche (1er groupe rencontré, les matchs arrivent déjà triés chronologiquement) s'affiche ouvert par défaut — les jours suivants sont repliés sous leur séparateur de date (clic sur le libellé pour dérouler, chevron qui pivote). État `openDays`, clé `${league}::${dateLabel}`. Code partagé par tous les pays/championnats (même bloc de rendu), donc actif partout sans logique séparée.
- **Fausse piste évitée** : la 1ère lecture de la demande initiale ("faire apparaître automatiquement les matchs à venir de ce championnat dans la side barre") avait été comprise comme "ajouter les matchs dans la sidebar `LeftNav.jsx`" — implémenté puis entièrement annulé sur retour utilisateur ("nimportek, je t'ai pas demandé ça"). La vraie demande portait sur le panneau pays de la Carte du Monde (`WorldMapPage.jsx`), pas la sidebar de navigation. Elle a ensuite été étendue en 2 temps : d'abord juste les pays à un seul championnat, puis (ce fix) tous les pays.

**Overlay Classement/leaders basket ouvert automatiquement sur l'onglet Basket (même session) :**
Quand l'onglet 🏀 Basket devient actif (clic explicite, ou déjà actif par défaut pour un pays où le basket est le 1er sport listé, ex. États-Unis), l'overlay `StatsOverlay` (Classement + PTS/REB/AST/3PM) s'ouvre désormais automatiquement au lieu d'attendre un clic sur le championnat — demande explicite, capture d'écran à l'appui. `_preferredBasketLeague` (dans `Panel`) choisit la ligue : **WNBA en dur** si présente parmi les ligues basket du pays (`_basketLeagues.includes('wnba')`), sinon la seule ligue basket dispo (ACB/LNB/BBL/Lega A). Pas de détection automatique de saison NBA/WNBA — préférence fixe assumée, l'utilisateur redemandera explicitement le switch vers NBA une fois la saison WNBA terminée. Câblé à 2 endroits : le `useEffect` de reset au changement de pays (`setStatsLeague(nextSport === 'basket' ? _preferredBasketLeague : null)`) et le handler de clic sur les icônes sport (même logique). Le dépliage d'un championnat déjà ouvert par défaut (cf. fix précédent) ne redéclenche pas l'overlay une 2e fois — comportement inchangé pour la fermeture (changer de sport ou re-cliquer l'icône active referme l'overlay comme avant).
- **Bug pré-existant découvert en testant, corrigé depuis** : le classement ACB affichait 3 équipes différentes sous le même sigle "BAS" (clés React dupliquées) — voir le fix "clés React dupliquées" juste en dessous, corrigé en étendant cet overlay au basket EU/foot le même jour.

**Overlay Classement/leaders étendu à tous les championnats (31 juillet 2026) :**
Demande explicite : construire le même overlay (Classement + catégories statistiques) pour le basket EU et le foot, pas seulement NBA/WNBA/ACB.
- **Basket EU (LNB/BBL/Lega A)** — `GET /api/euro/:league/leaders` (`backend/server.js`), nouveau : roster de chaque équipe (`bballFetch('/players?team=...')`) → gamelog de chaque joueur (`bballPlayerGamelog`, déjà utilisé/caché pour les props) → moyenne pts/reb/ast/3pm sur la saison → top 5 par catégorie. Cache 6h, persisté disque (`euro_basket_leaders.json`). Jamais appelé en tâche de fond (uniquement à la demande, à l'ouverture de l'overlay) — pas le piège quota du 28 juillet (`getEuroDefByPos`). `GET /api/euro/:league/standings` (générique, existait déjà mais jamais consommé côté frontend) réécrit pour renvoyer la même forme que `/api/acb/standings` (`{standings:[{id,rank,abbr,logo,wins,losses,pct,gb}]}`) — ACB garde son scraping dédié acb.com (plus complet), les 3 autres n'ont pas d'alias d'abréviation dédié (repli 3 premières lettres du nom).
- **Foot (5 grands championnats + Brasileirão)** — Classement : `GET /api/football/standings/:league` existait déjà (football-data.org), juste câblé dans l'overlay (+ `crest` ajouté à la réponse). Buteurs/Passeurs : nouveau `GET /api/football/topscorers/:league`, api-football Pro `/players/topscorers` + `/players/topassists` — league-wide déjà trié côté API, pas d'agrégation joueur par joueur à faire (plus simple que le basket EU). **Pas de repli sur la saison précédente** (décision explicite, tranchée après un aller-retour le jour même) : un 1er essai retombait automatiquement sur la saison N-1 quand la saison en cours n'avait encore aucune donnée (creux hors-saison mi-juillet/mi-août) pour éviter un widget vide — mais ça affichait silencieusement les stats d'une saison terminée à côté d'un Classement à 0 partout (incohérent). Utilisateur a tranché pour la cohérence : Buteurs/Passeurs restent vides tant que la vraie saison en cours n'a aucune donnée, exactement comme le Classement. **Pas les 3 coupes d'Europe** (décision explicite utilisateur — pas de classement unique pour elles, groupes puis élimination directe).
- **Frontend (`WorldMapPage.jsx`)** : `StatsOverlay` généralisé — `isFootball` bascule les colonnes du classement (J/V/N/D/PTS au lieu de V/D/.PCT/GB) et le nombre de catégories (2 `FOOTBALL_CATS` Buteurs/Passeurs au lieu de 4 `STAT_CATS`) ; toggle "Conf." limité à NBA/WNBA (`hasConferences`, plus à `league !== 'acb'`). `_preferredBasketLeague`/`_preferredFootballLeague` dans `Panel` déterminent quelle ligue ouvrir automatiquement selon le sport actif (généralisé en `_preferredStatsLeague(sport)`).
- **Bug trouvé et corrigé (clés React dupliquées)** : plusieurs clubs LNB/ACB partagent la même abréviation générée (repli 3-lettres) → clé `t.abbr` dupliquée. Fix : `id` (numérique, garanti unique) ajouté à `/api/acb/standings`, `/api/euro/:league/standings` et à la transformation du classement foot ; `StandTable` utilise `key={t.id ?? t.abbr}` (repli conservé pour NBA/WNBA qui n'ont pas d'`id` mais des abréviations déjà uniques).
- **Bug trouvé et corrigé (changement de pays cassait l'overlay)** : le bouton pays de la légende n'appelait pas `e.stopPropagation()` — un clic pendant que l'overlay était ouvert bulle jusqu'au conteneur racine (`onClick` qui ferme tout si `statsLeague` est actif) et annulait la sélection du nouveau pays dans la même passe. Fix : `stopPropagation()` ajouté. Deuxième bug lié : un vieil effet parent (`WorldMapPage`, hérité de la décision du 24 juillet "jamais d'auto-ouverture") faisait encore `setStatsLeague(null)` à chaque changement de `selected`, entrant en course avec le nouvel effet de montage de `Panel` et annulant l'auto-ouverture par défaut (sans clic sur l'icône sport) un coup sur deux — supprimé, `Panel` est désormais seul responsable de `statsLeague`.
- **Limite connue (pas corrigée)** : pas de photo joueur pour le basket EU (api-sports.io basketball ne fournit pas ce champ pour LNB/BBL/Lega A, contrairement au foot où api-football les fournit) — cases vides à la place, cosmétique uniquement.

**Basket EU — vraie cause des "stats de la saison dernière" (31 juillet 2026, même jour) :**
`EURO_LEAGUES.acb/lnb/bbl/legaa.season` est une chaîne fixée à la main (`'2025-2026'`) — jamais recalculée automatiquement. Résultat : Classement ET leaders affichaient les **vraies stats de la saison qui vient de se terminer** comme si c'était la saison en cours, contrairement au foot (football-data.org gère lui-même la transition de saison, `/api/football/standings` renvoyait déjà honnêtement des 0 pour la nouvelle saison — cf. fix juste au-dessus). Vérifié en direct : demander la saison suivante (`2026-2027`) à api-sports.io renvoie 0 équipe/0 classement — la saison n'existe simplement pas encore chez eux, impossible de juste "demander la nouvelle saison" comme pour le foot.
- **Fix** : `euroBasketCurrentSeason(dateStr)` calcule la vraie saison en cours par la date (seuil juillet, même convention que `footballApiSeasonForDate`) ; `isEuroBasketSeasonCurrent(league)` compare au `cfg.season` figé. Tant que ça ne correspond pas : `/api/acb/standings` et `/api/euro/:league/standings` continuent d'afficher les **équipes** (identité/roster stable l'été, vient de `cfg.season`) mais avec **wins/losses/pct/gb/rank neutralisés à 0/1** ; `/api/euro/:league/leaders` renvoie entièrement vide (pas d'équivalent "identité sans les stats" pour un classement de joueurs, même décision que Buteurs/Passeurs foot).
- **Important — ne touche PAS `cfg.season` lui-même** : ce fix agit uniquement sur l'affichage (3 endpoints), pas sur la valeur partagée utilisée partout ailleurs (rosters, gamelogs, moteur de props/alertes EU). `cfg.season` devra être mis à jour à la main (comme l'ordre WNBA/NBA plus haut) quand la vraie saison 2026-2027 démarrera réellement chez api-sports.io — à ce moment-là, `isEuroBasketSeasonCurrent` repassera automatiquement à `true` sans autre changement de code, et l'overlay recommencera à afficher les vraies stats en direct.

**Big Five foot — même bug, cause différente (classement périmé déjà connu)** : signalé par l'utilisateur juste après le fix basket EU (capture d'écran La Liga : 38 matchs joués, classement complet de la saison qui vient de se terminer, sous une saison techniquement déjà nouvelle). Cause différente du basket EU cette fois — c'est exactement le **bug du 28 juillet 2026** déjà documenté plus haut ("classement périmé"), pour lequel `_isFdStandingsStale()`/`_fdSeasonInfo` avaient déjà été construits, mais **seulement pour le moteur d'alertes outrights** — jamais branchés sur `GET /api/football/standings/:league`, la route utilisée par ce nouvel overlay. Ligue1/Serie A/Brasileirão n'ont pas ce problème (déjà à 0 joué chez football-data.org), PL/La Liga/Bundesliga si.
- **Fix** : `_isFdStandingsStale(data.season?.currentMatchday, table)` recalculée directement dans `/api/football/standings/:league` (indépendamment de `_fdSeasonInfo`, pas garanti synchro/peuplé pour toutes les ligues) — si périmé, toutes les stats de chaque équipe (points/played/wins/draws/losses/goalsFor/goalsAgainst/form/position) sont neutralisées à 0/1, même traitement que le basket EU juste au-dessus. Se corrige tout seul dès que football-data.org réinitialise vraiment son tableau — rien à surveiller manuellement ici, contrairement au basket EU.

**Fond d'écran "Nébuleuse bleue" ajouté (31 juillet 2026, `src/components/StarField.jsx`) :**
Demande explicite : porter le fond d'écran d'un autre projet de l'utilisateur, **PulseSignal** (`/Users/sacha/Desktop/Claude projets/Projets marchés/`, maquette non liée à ValueBet — même composant `StarField.jsx`, dupliqué pas partagé entre les deux projets). 1ère tentative faite en devinant depuis une simple capture d'écran (fond "sobre, juste des étoiles") — **incorrecte**, corrigée après que l'utilisateur ait donné le chemin exact du vrai fichier source. Le vrai fond (`MarketNebulaBackground`) est structurellement identique à "Nébuleuse spatiale" (glow radial + étoiles montantes) mais en palette bleu/cyan/émeraude au lieu de violet/magenta — juste plus discret visuellement à l'écran, pas dépourvu de glow coloré comme supposé au 1er essai. Ajouté comme **7e variante, en position 1** (donc par défaut) dans le switcher existant, les 6 fonds précédents restent tous accessibles via les flèches.
- **Leçon** : toujours privilégier le vrai fichier source à une capture d'écran quand l'un est disponible — l'écart entre les deux tentatives ici était entièrement dû à une couleur de glow trop proche du fond pour être visible clairement sur la capture, pas à une différence réelle de structure.

**Correction surconfiance points WNBA (1er août 2026, `backend/compute.js` + miroir `src/pages/BasketballDetailPage.jsx`) :**
Investigation déclenchée par l'utilisateur suite à un pari réel (Arike Ogunbowale, Over 14.5/15.5 pts) : le near-miss montrait le modèle **systématiquement surconfiant** sur les points WNBA à toutes les tranches de probabilité (ex: 65% affiché → 55,6% de réussite réelle, -9,5pp d'écart) — l'inverse exact des rebonds (sous-confiants, d'où le test réussi à 68% du 30 juillet). Deux causes structurelles trouvées, propres aux points (jamais aux reb/ast/tpm) :
- **Cause 1 — un filtre qui cachait les mauvaises soirées** : `calcStd`/`isConsistentStat`/`blendedSeasonAvg`/le calcul EWA de `computeEstimate` excluaient les matchs à 0 pts avec ≥12 min (fautes, tir glacial, sortie précoce) — traités comme des "outliers" à retirer, alors que c'est de la vraie variance à voir. Aucune exclusion équivalente n'existait pour reb/ast/tpm. Fix : filtre retiré, points traités comme les autres stats (`g.min > 10 && g[key] != null`).
- **Cause 2 — l'ancrage volume×efficacité (`getShotVolumeAnchor`, 15 juin) ne touchait que la moyenne, jamais l'incertitude** : ce mécanisme (blend 85/15 avec la projection principale) pousse la moyenne vers le haut pendant une série chaude au tir, mais son signal n'était jamais reflété dans `deviation.pts` (jusqu'ici `|adjMult-1|`, qui ne voit que les facteurs AVANT le blend volAnchor) — donc `probAtLeast` n'élargissait jamais le std en conséquence. Fix : `deviation.pts` recalculée comme l'écart réel final `|projPts/basePts - 1|`, qui capture le mouvement total (adjMult + volAnchor).
- **Deux corrections déjà tentées avant (18-20 juillet, shrinkage pts renforcé à 0.35 vs 0.12-0.25 pour les autres stats, cap adjMult resserré 1.24→1.12) n'avaient pas suffi** — ces deux nouvelles causes sont les vrais points morts non traités jusqu'ici.
- **Garde-fou explicite de l'utilisateur respecté** : aucune des deux corrections ne touche au système de détection "joueuse chaude" (forme récente, EWA, volAnchor lui-même) — la projection continue de monter normalement pendant une série chaude, seule l'honnêteté de la confiance autour change.
- **Miroir frontend** : `calcStd`/le filtre gClean dans `BasketballDetailPage.jsx` corrigés à l'identique (Cause 1). Le frontend n'a pas de `getShotVolumeAnchor`/volAnchor du tout (divergence backend/frontend pré-existante, hors scope ici) donc Cause 2 ne s'y applique pas.
- **Vérification** : script Node autonome confirmant que le mauvais match (0 pts, 15 min) est maintenant bien inclus dans `calcStd` (8 matchs comptés au lieu de 7, std recalculé en conséquence) ; cycle bg-alerts complet post-fix sans erreur (`wnba props OK for PORvIND: 11 players`) ; page match WNBA vérifiée en direct sans NaN ni crash. **Pas de comparaison numérique avant/après sur un vrai near-miss** possible : `_logNearMissCandidate` dédupe par `gameId_player_stat_direction_line` et ne réécrit jamais un candidat déjà loggé (pour ne pas corrompre le suivi de calibration) — l'effet réel du fix ne sera visible que sur les nouveaux candidats à venir.
- **Conséquence attendue, pas encore actée** : cette correction va probablement faire *baisser* les probabilités points affichées à court terme (elles étaient gonflées) — c'est voulu, ça permettra d'envisager plus tard un abaissement du seuil 77% en toute confiance (comme pour reb), une fois le near-miss reconstitué sur la formule corrigée. Ne pas toucher `WNBA_ALERT_FLOOR.pts` avant d'avoir un nouvel échantillon net.

---

## Session 1er août 2026 — planchers WNBA, fix ligne figée, unification props, calculateur de mise

**Planchers passes/3pts WNBA abaissés en test encadré + fix inversion plancher spécialiste (`backend/server.js`) :** même constat que le test rebonds du 30 juillet (0 alerte props sur aucune stat depuis le 19/07, plafond de confiance du modèle naturellement ~65-70% après les resserrements de calibration du 19-21 juillet) — étendu à ast (`WNBA_AST_FLOOR_TEST=0.72`, original 0.80) et tpm (`WNBA_TPM_FLOOR_TEST=0.65`, original 0.73, déjà baissé une 1ère fois le 20 juillet). Même filet de sécurité que reb : fichiers d'état dédiés (`wnba_ast_floor_test.json`/`wnba_tpm_floor_test.json`), rollback auto si ROI négatif sur ≥30 candidats résolus, notif Telegram si rollback. Points exclus (formule corrigée le même jour, pas de données post-fix valides pour juger un nouveau seuil).
- **Bug d'inversion trouvé et corrigé en même temps** : `WNBA_SPECIALIST_FLOOR` était un flat `0.72` pour les 4 stats — une fois les planchers titulaires reb/ast/tpm passés sous 0.72, être "spécialiste" (`isConsistentStat`) devenait un désavantage (ex: tpm titulaire 65% < spécialiste 72%). Transformé en objet par stat (`reb:0.60, ast:0.64, tpm:0.60`, branché sur le même flag actif que chaque test — pas de 2e filet de sécurité séparé).

**Alerte props avec ligne figée pendant que proba/cote suivaient la ligne live (`refreshOrDropPendingProp()`, `backend/server.js`) :** cas réel signalé (Pauline Astier, carte affichant "Over 2.5 passes" alors que la vraie ligne Unibet était 3.5 à 2.30) — cette fonction recalculait `probability`/`unibetOdds`/`betclicOdds` contre la ligne de référence **actuelle** à chaque cycle sans jamais mettre à jour le champ `line` affiché, figé sur la ligne de création. Fix : nouveau paramètre `currentLine` — si la ligne bookmaker a bougé depuis la création (et que l'alerte n'est pas `lineSource:'ladder'`, qui vit délibérément sur une autre ligne), l'alerte est abandonnée comme une cote devenue invalide plutôt que réétiquetée en silence ; la génération normale recréera une alerte fraîche sur la nouvelle ligne si elle qualifie encore. Branché aux 5 sites d'appel (NBA live/frozen, WNBA live/frozen, EU).

**Perfs joueurs — fiche de match unifiée avec le vrai moteur d'alertes (cf. section dédiée plus haut, "Player props — match-page display unified") :** voir cette section pour le détail complet (nouvel endpoint `/api/basketball/player-projections`, `getPlayerProjectionsNBA/WNBA/EU` dans `server.js`, champ `factors` ajouté à `computeEstimate`).

**Calculateur de mise multi-alertes (`src/pages/PlaceBetPage.jsx`, `StakeCalculatorWidget`) :** bouton 🧮 flottant bas-droite de la page Alertes — outil **manuel et instantané** (pas un partage automatique inter-jours, cf. tentative abandonnée du 16 juillet dans `bankroll.js` : impossible de deviner à l'avance si une alerte va sortir plus tard dans la journée). Ne regarde que les alertes déjà affichées à l'instant où on l'ouvre (`allPendingItems`), donc aucune prédiction du futur nécessaire — problème différent, pas une contradiction avec la décision de juillet.
- `extractProbOdds(item)` normalise `{label, probability, odds}` depuis n'importe quel type d'alerte pending (props, foot BTTS/Total/Résultat, basket Résultat/Écart H2H/Total, value bets Pinnacle) — champs non-uniformes selon le type (`probability` vs `prob`) gérés au cas par cas. Outrights exclus (pari saison entière).
- Lit `loadBankrollState().current` → palier `BANKROLL_BRACKETS` (budget du jour) et `getEngagedToday()` (déjà engagé) pour calculer le budget restant.
- **Pondération par fraction de Kelly, pas edge brut** (`f=(bp-q)/b`, b=cote-1) — corrigé suite à une question de l'utilisateur sur le risque de sur-miser la plus grosse cote : avec un edge brut, plusieurs alertes à la même probabilité affichée (ex: plafond `sanityMax` à 65%) finissaient par juste recevoir plus d'argent proportionnellement à la cote la plus haute, sans tenir compte de la variance. **Demi-Kelly** (`KELLY_FRACTION=0.5`) retenu plutôt que Kelly plein — nos probabilités affichées ne sont jamais parfaitement calibrées (tests de seuils en cours), Kelly plein suppose une proba exacte et sur-mise si elle est fausse.
- **Sélecteur de palier simulé** — `bracketOffset` (état local, remis à 0 à la fermeture) permet de recalculer sur un palier plus prudent ou plus agressif que le bankroll réel actuel (boutons ‹ ›, libellé "(simulé)", lien "↺ réel") sans jamais toucher au bankroll réellement suivi (Suivi Bankroll, `BacktestingPage.jsx`).
- Réactif automatiquement : rejeter une alerte la fait disparaître du calcul instantanément (pas de snapshot interne, recalcul à chaque rendu depuis `allPendingItems` déjà filtré `pending`). Rien n'est écrit automatiquement sur les alertes — purement indicatif.

---

## Session 2 août 2026 — polish UI Outrights, alertes division/finaliste, effet d'ouverture de page

**Outrights — cotes Pinnacle en bleu + alignement titre/books (`OutrightsPage.jsx`, `OutrightCompetitionPage.jsx`) :** demande explicite utilisateur, 3 volets sur le composant `Card` partagé (donc appliqué d'un coup à la page grille principale ET à la fiche détail par compétition) :
- `BOOK_ODDS_COLORS.pinnacle` passé de `#ffffff` à `#60a5fa` — même bleu que `isFavorite` sur une ligne d'équipe (ex. Paris SG surligné), confirmé par mesure de pixels avant de généraliser.
- Titre du marché (`VAINQUEUR`, `TOP 4`…) déplacé en `gridColumn: '1 / span 2'` pour démarrer à la même position que la colonne des numéros de rang, pas celle des noms d'équipe — clarifié après une 1ère tentative où le titre était déjà (et restait) aligné avec les noms d'équipe, ce qui n'était pas ce que l'utilisateur voulait.
- Labels des books (`PINNACLE`/`BETCLIC`/`PMU`) passés de `textAlign:'left'` à `'right'` pour s'aligner avec les cotes (déjà `textAlign:'right'`) juste en dessous.
- **Bug backend trouvé au passage** — voir la section "Alertes sur les marchés secondaires" plus haut (fix cache Pinnacle par marché).

**Effet d'ouverture de page — toutes les pages de la side barre (`src/index.css`, plusieurs pages) :** demande explicite : le même effet visuel que "Sport" (`WorldMapPage.jsx`, `mapReveal`) et "Base de données" (`DatabaseMapPage.jsx`, `dbMapReveal`) sur Dashboard/Backtesting/Alertes/Running/Outrights. Ces deux pages ont leur propre reveal (scale 0.96→1 + fondu, 0.8s ease-out) laissé **inchangé**. Pour les 5 pages sidebar restantes : la classe partagée `.page` existait déjà (utilisée par Backtesting/Alertes/Running/MatchDetailPage/BasketballDetailPage) mais avec l'ancien `pageFadeIn` — un fondu seul, 0.18s, jugé trop discret pour être remarqué. `pageFadeIn` remplacé par un nouveau keyframe `pageEnterFade` reprenant exactement le motif scale+fondu de `mapReveal`/`dbMapReveal` (0.8s ease-out) — une seule modif de `.page` met donc à jour Backtesting/Alertes/Running (déjà la classe) d'un coup ; `className="page"` ajouté en plus sur `DashboardPage.jsx`, `OutrightsPage.jsx`, `OutrightCompetitionPage.jsx` (n'avaient aucune classe avant). Effet secondaire assumé : `MatchDetailPage`/`BasketballDetailPage` (déjà sur `.page`, pas dans la side barre) héritent aussi du nouvel effet — cohérence visuelle plutôt que risque, aucune régression.
- **1er essai rejeté** : un slide+fondu maison (`translateY`+opacity, 0.28s) proposé puis testé sur Dashboard seul — jugé pas assez satisfaisant, l'utilisateur a demandé explicitement de reprendre le même style que Sport/Base de données plutôt que d'itérer sur un nouveau style.
- **Glitch signalé sur Backtesting non reproduit** : l'utilisateur a remarqué un petit accroc visuel juste avant/pendant le fondu, spécifique à cette page. Testé en détail (captures à intervalles réguliers sur l'animation réelle et ralentie, revue des états qui pourraient causer un décalage de mise en page) sans rien reproduire de concluant — laissé de côté sur demande explicite de l'utilisateur plutôt que de continuer à deviner. À reprendre si le problème se represente, avec une description plus précise (vidéo/capture) ou en le reproduisant soi-même.

**Incident dev — backend `--watch` décroché (voir aussi `feedback_backend_watch_autoreload` en mémoire) :** en vérifiant les nouvelles alertes Division NBA/Finaliste WNBA, aucune n'apparaissait alors que le code semblait correct. Cause : le process qui répondait sur le port 3001 était l'enfant d'un superviseur `node --watch` lancé **vendredi**, qui avait cessé de redémarrer sur sauvegarde de fichier silencieusement (heure de démarrage du process postérieure à la dernière modif réelle du fichier, confirmé via `ps`/`stat`) — tous les changements faits depuis n'étaient donc jamais réellement exécutés, malgré un `/api/health` qui répondait normalement. Une bonne douzaine d'autres processus `node --watch server.js` zombies (accumulés sur plusieurs jours, `jeudi`→`aujourd'hui`) traînaient aussi sans rien écouter. Fix : tuer tout le tas, relancer proprement via `npm run dev`. **Symptôme à retenir** : si un fix censé être trivial ne se manifeste jamais après une sauvegarde, comparer l'heure de démarrage du process (`ps -o lstart`) à la date de modif du fichier (`stat`) avant de chercher un bug dans le code lui-même.

**Documentation** : nouvelle section "Outrights (paris longterme)" ajoutée à l'onglet Utilisation (`src/pages/UtilisationPage.jsx`) — jusque-là totalement absente de la doc utilisateur malgré la feature construite depuis le 23 juin. Couvre sources/marchés par compétition/types d'alertes/seuils/règlement + les 2 fixes de ce jour (cache Pinnacle, classement NBA périmé).

---

## Session 20 août 2026 — audit Q/OUT complet, uniformisation Résultat/Total/Écart H2H, 3 bugs de synchro corrigés

**Contexte déclencheur** : alerte "Allisha Gray, Over 19.5 pts" affichant `oppQSamePosition: true` ("Adversaire Q au même poste, pas de boost appliqué") — l'utilisateur s'inquiétait que la logique de statut Q/OUT soit incohérente à travers le projet. Un audit exhaustif (fork dédié) a listé les 7 mécanismes existants (NBA/WNBA/EU) et confirmé qu'aucun n'était buggé (le badge Allisha Gray était le comportement normal, badge informatif sans aucun effet sur le calcul), mais a révélé une vraie incohérence entre marchés : les gates props (statut du joueur lui-même, coéquipière, adversaire) sont fins et directionnels, alors que Résultat/Total/Écart H2H utilisaient un gate binaire "tout ou rien" avec un critère de déclenchement différent du Total. Détail livré à l'utilisateur sous forme d'artifact (`Garde-fous Q/OUT`).

**Uniformisation Résultat / Total (Points) / Écart H2H (`backend/server.js`) — 3 changements, sur demande explicite après discussion des options :**
1. **Seuil "joueur clé" unifié à `KEY_PLAYER_PTS` (≥15 pts/match)** sur les 3 marchés — Résultat/Écart H2H utilisaient jusqu'ici "titulaire top-5 minutes" (`homeStartersRes`/`awayStartersRes`, supprimés), un critère différent de celui de leur propre pénalité Out (`calcKeyPlayerOutPenalty`, déjà sur ≥15pts) et de celui de Total. Un titulaire défensif à 8 pts/match bloquait Résultat/Écart sans raison réelle ; un 6e homme à 18 pts/match (non-titulaire) leur échappait entièrement.
2. **Gate Résultat/Écart H2H rendu asymétrique** — un joueur clé Q/GTD dans une équipe (`homeHasKeyQ`/`awayHasKeyQ`) ne bloque plus que les alertes SUR cette équipe (`side === 'home' && homeHasKeyQ` etc., dans les boucles de création ET de `refreshOrDropPendingById`) ; l'adversaire reste alerté normalement. Raisonnement validé avec l'utilisateur : si le joueur clé finit par ne pas jouer, les chances de l'adversaire ne font que s'améliorer par rapport à ce que `computeTeamWinProb` calcule aujourd'hui (qui suppose encore l'équipe au complet tant que rien n'est confirmé Out) — bloquer ce sens-là coûtait un edge réel pour rien. Appliqué en parallèle sur les blocs NBA/WNBA (`leagueKey`) et EU (`euLeague`) — code dupliqué entre les deux, même fix des deux côtés. `dropPendingOnKeyPlayerQ(gameId, idPrefix, sides)` prend maintenant un 3e paramètre pour ne purger que le(s) côté(s) concerné(s) (défaut `['home','away']` pour compat). **Total reste un blocage total** (Over ET Under) — pas de "côté" à exempter sur un marché combiné (score cumulé, pas par équipe).
3. **Pénalité Out confirmé limitée aux absences récentes** — `filterSeasonLongOut` (8 juillet 2026, excluait seulement les absences "depuis le début de la saison") remplacée par `filterRecentlyOut(players, gamelogFn, gameDate)` : exclut désormais tout joueur dont le dernier match joué remonte à plus de `RECENT_OUT_DAYS` (3 jours, non calibré) **avant le match évalué** — pas un nombre de matchs manqués (un jour reste un jour peu importe la fréquence du calendrier de l'équipe), et pas "maintenant" mais la date du match (reste correct en évaluant un match dans quelques jours). Raisonnement (proposé par l'utilisateur, validé) : passé quelques jours, l'absence est déjà largement reflétée dans le rating récent de l'équipe (EWA sur les derniers matchs) — garder la pénalité pleine double-compte de plus en plus à mesure que l'absence s'étire, pas seulement au cas extrême "toute la saison" déjà corrigé le 8 juillet (qui en devient un cas particulier, capturé automatiquement : un écart de jours "infini" dépasse toujours 3). Branché sur les **7 points d'entrée** : les 3 endpoints d'affichage (`/api/basketball/result`, `/total`, `/spread`) + les 4 boucles background (Résultat/Total NBA-WNBA, Résultat/Total EU) — même garantie "page = alerte" que le reste de l'app.
- **40% (`KEY_PLAYER_OUT_FACTOR`) et 3 jours (`RECENT_OUT_DAYS`) non calibrés** — même statut que `DIXON_COLES_RHO`/`MLB_HOME_ADV` : points de départ raisonnés (redistribution partielle de la production vers les coéquipiers ; fenêtre EWA courte), jamais vérifiés sur des résultats réels. Impossible de vérifier a posteriori avec les données near-miss actuelles (aucun tag "pénalité Out appliquée" sur les candidats loggés) — à ajouter si une vraie calibration est voulue un jour.

**3 bugs de synchro frontend trouvés et corrigés en marge (`src/utils/syncAlerts.js`, `syncBackgroundAlerts()`) — cas réel Allisha Gray acceptée via Telegram, jamais affichée en Running malgré des données backend correctes :**
1. `status: prev?.status || a.status || 'pending'` (fix du 2 août) ne servait à rien dans son propre contexte : on n'entre dans cette branche que quand `prev` est absent OU déjà `'pending'` (garde de la condition englobante), donc `prev?.status` valait toujours soit `undefined` soit `'pending'` — une chaîne non vide, donc toujours gagnante face à `a.status` dans le `||`. Un accept fait depuis Telegram (jamais écrit en local) restait donc bloqué `'pending'` indéfiniment si une copie locale pending existait déjà. Fix : `status: a.status || 'pending'` — le backend fait foi dans cette branche, jamais l'inverse.
2. La détection de "changement" (qui décide si `cloudSet` réécrit le localStorage) ne comparait jamais le statut, seulement probabilité/ligne/cotes/`playerIsQ` — donc même une fois le point 1 corrigé, si rien d'autre n'avait bougé depuis la dernière synchro pending, `changed` restait `false` et l'écriture n'avait jamais lieu. Fix : `(prev.status || 'pending') !== (a.status || 'pending')` ajouté à la liste de comparaison.
3. **Cause racine réelle** : une alerte acceptée qui existe uniquement dans `nba_bet_history` (jamais dans `nba_prop_alerts` courant) n'était restaurée que si une cote/ligne/proba avait bougé depuis (`acceptedMatch` trouvé par empreinte, mais `byId[acceptedMatch.id]` seulement posé à l'intérieur du `if (lineShift || ubShift || ...)`) — si rien n'avait changé (cas fréquent, l'alerte n'a pas eu le temps de dériver entre création et acceptation Telegram), l'entrée n'était jamais recopiée dans la liste courante. Fix : `needsRestore = !byId[acceptedMatch.id]` ajouté à la condition de restauration.
- Ces 3 fixes n'ont pas suffi à eux seuls à faire réapparaître l'alerte Allisha Gray dans Running en direct pendant la session (un onglet resté ouvert avec l'ancien code continuait de réécrire Mongo par-dessus) — restaurée manuellement via patch direct de `/api/userdata` (`nba_prop_alerts`) une fois les 3 fixes livrés, cf. `feedback_manual_alert_restore_via_userdata` en mémoire pour la méthode.

**Documentation mise à jour en 3 endroits** (même session, sur demande explicite) :
- `OddsLegendCard` (`BasketballDetailPage.jsx`, tooltip "?" des onglets Résultat/Points/Écart H2H) — les 3 textes `ALERTS.all`/`.points`/`.handicap` reflètent maintenant le seuil ≥15pts, la pénalité Out ≤3j et le gate asymétrique.
- `UtilisationPage.jsx` — table "Garde-fou spécifique" de la section basket mise à jour + nouvelle sous-section "Uniformisation des sécurités Résultat / Total / Écart H2H (20 août 2026)".
- Ce fichier (session courante).

---

## Session 27 août 2026 — MLB et Écart H2H retirés du projet

Demande explicite de l'utilisateur, exprimée avec insistance ("on va supprimer le marché ECART... LA MLB et tout ce qui sy rapporte doit également etre supprimé, on va alleger tout ca j'en ai marre la") : suppression complète (pas une simple pause/flag) de deux marchés.

- **MLB** : retiré intégralement — `computeMlb.js`, toutes les routes/scrapers/caches `mlb_*` (backend), `MlbDetailPage.jsx`, icône ⚾ Carte du Monde, panneau MLB de Base de données, near-miss dédié (`near_miss_mlb.json`). Suppression sûre : `MLB_ALERTS_ENABLED` était resté `false` depuis sa création (24 juillet), jamais une seule alerte réelle émise, zéro entrée MLB dans `bet_ledger.json`.
- **Écart H2H (basketball_spread)** : toute la génération/affichage/synchro d'alertes retirée (backend : constantes `SPREAD_*`, `computeSpreadCoverProb`, `findSpreadLadderAlternative`, la route `/api/basketball/spread`, la boucle de création NBA/WNBA/EU, le settlement dédié ; frontend : `BasketballSpreadCard`/state/sync dans `PlaceBetPage.jsx` et `RunningPage.jsx`, `syncBasketballSpreadAlerts` dans `syncAlerts.js`, l'entrée Telegram `ALERT_TYPES.basketball_spread`). Le mécanisme de corrélation `matchCorrelation` (Résultat↔Écart H2H) a été supprimé avec lui ; `hasOpposingAcceptedTeamMarket`/`opposingPositionWarning`/`teamDirectionalAlerts` sont conservés, juste réduits à `basketball_result`/`game_total`. `computeTeamWinProb`, `BB_RESULT_MAX_PROB` et le scraping des lignes (`spreadAllLines`, utilisé ailleurs) sont intacts.
  - **Historique financier préservé à l'identique** : 14 vrais paris Écart H2H (8 gagnés, 6 perdus, tous déjà réglés) restent dans `bet_ledger.json` et dans les clés localStorage/cloud (`basketball_spread_alerts`) qui alimentent Backtesting/Dashboard/Suivi Bankroll — ces clés et leur code d'affichage n'ont volontairement pas été touchés, seule la génération de *nouvelles* alertes a été retirée.

---

## Adding fixtures or leagues

- **Football static data**: edit `src/utils/fixtures.js`. The `LEAGUES` array controls ordering and accent colors.
- **Basketball static data**: edit `src/utils/basketball.js`. Après chaque modif, mettre à jour ce fichier CLAUDE.md ET `memory/project_nba_data.md`.
- **CONVENTION DATES NBA** : les matchs jouent en ET (UTC-4 en été). Toujours convertir en UTC pour le champ `date` :
  - 20h30 ET → lendemain T00:30:00Z (ex: 25 mai 20h30 ET → `2026-05-26T00:30:00Z`)
  - 21h00 ET → lendemain T01:00:00Z
  - Ne jamais écrire la date ET telle quelle — c'est la source du bug "match détecté terminé avant d'avoir lieu".
- **Live football leagues**: add entries to `FOOTBALL_LEAGUES` (server.js) and `ODDS_SPORTS` for odds coverage.
