Reprends le projet ValueBet (dashboard paris sportifs, React + Vite + Express).

1. Lis `CLAUDE.md` à la racine du projet pour le contexte architecture.
2. Trouve les fichiers récemment modifiés :
   ```
   find . \( -name "*.jsx" -o -name "*.js" -o -name "*.css" \) -newer package.json -not -path "*/node_modules/*" -not -path "*/dist/*"
   ```
3. Résume en **2-3 lignes max** ce qui a été fait lors de la dernière session (fichiers touchés + fonctionnalité ajoutée).
4. Demande : "Sur quoi on travaille aujourd'hui ?"

Pas de longue description de l'architecture, pas de liste exhaustive. Juste l'essentiel et la question.
